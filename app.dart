import "dart:math";


class Model0 {
  final int id;
  const Model0(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1 {
  final int id;
  const Model1(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2 {
  final int id;
  const Model2(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model3 {
  final int id;
  const Model3(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model4 {
  final int id;
  const Model4(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model5 {
  final int id;
  const Model5(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model6 {
  final int id;
  const Model6(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model7 {
  final int id;
  const Model7(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model8 {
  final int id;
  const Model8(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model9 {
  final int id;
  const Model9(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model10 {
  final int id;
  const Model10(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model11 {
  final int id;
  const Model11(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model12 {
  final int id;
  const Model12(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model13 {
  final int id;
  const Model13(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model14 {
  final int id;
  const Model14(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model15 {
  final int id;
  const Model15(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model16 {
  final int id;
  const Model16(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model17 {
  final int id;
  const Model17(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model18 {
  final int id;
  const Model18(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model19 {
  final int id;
  const Model19(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model20 {
  final int id;
  const Model20(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model21 {
  final int id;
  const Model21(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model22 {
  final int id;
  const Model22(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model23 {
  final int id;
  const Model23(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model24 {
  final int id;
  const Model24(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model25 {
  final int id;
  const Model25(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model26 {
  final int id;
  const Model26(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model27 {
  final int id;
  const Model27(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model28 {
  final int id;
  const Model28(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model29 {
  final int id;
  const Model29(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model30 {
  final int id;
  const Model30(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model31 {
  final int id;
  const Model31(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model32 {
  final int id;
  const Model32(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model33 {
  final int id;
  const Model33(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model34 {
  final int id;
  const Model34(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model35 {
  final int id;
  const Model35(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model36 {
  final int id;
  const Model36(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model37 {
  final int id;
  const Model37(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model38 {
  final int id;
  const Model38(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model39 {
  final int id;
  const Model39(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model40 {
  final int id;
  const Model40(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model41 {
  final int id;
  const Model41(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model42 {
  final int id;
  const Model42(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model43 {
  final int id;
  const Model43(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model44 {
  final int id;
  const Model44(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model45 {
  final int id;
  const Model45(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model46 {
  final int id;
  const Model46(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model47 {
  final int id;
  const Model47(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model48 {
  final int id;
  const Model48(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model49 {
  final int id;
  const Model49(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model50 {
  final int id;
  const Model50(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model51 {
  final int id;
  const Model51(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model52 {
  final int id;
  const Model52(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model53 {
  final int id;
  const Model53(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model54 {
  final int id;
  const Model54(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model55 {
  final int id;
  const Model55(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model56 {
  final int id;
  const Model56(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model57 {
  final int id;
  const Model57(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model58 {
  final int id;
  const Model58(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model59 {
  final int id;
  const Model59(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model60 {
  final int id;
  const Model60(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model61 {
  final int id;
  const Model61(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model62 {
  final int id;
  const Model62(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model63 {
  final int id;
  const Model63(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model64 {
  final int id;
  const Model64(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model65 {
  final int id;
  const Model65(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model66 {
  final int id;
  const Model66(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model67 {
  final int id;
  const Model67(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model68 {
  final int id;
  const Model68(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model69 {
  final int id;
  const Model69(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model70 {
  final int id;
  const Model70(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model71 {
  final int id;
  const Model71(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model72 {
  final int id;
  const Model72(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model73 {
  final int id;
  const Model73(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model74 {
  final int id;
  const Model74(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model75 {
  final int id;
  const Model75(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model76 {
  final int id;
  const Model76(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model77 {
  final int id;
  const Model77(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model78 {
  final int id;
  const Model78(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model79 {
  final int id;
  const Model79(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model80 {
  final int id;
  const Model80(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model81 {
  final int id;
  const Model81(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model82 {
  final int id;
  const Model82(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model83 {
  final int id;
  const Model83(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model84 {
  final int id;
  const Model84(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model85 {
  final int id;
  const Model85(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model86 {
  final int id;
  const Model86(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model87 {
  final int id;
  const Model87(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model88 {
  final int id;
  const Model88(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model89 {
  final int id;
  const Model89(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model90 {
  final int id;
  const Model90(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model91 {
  final int id;
  const Model91(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model92 {
  final int id;
  const Model92(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model93 {
  final int id;
  const Model93(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model94 {
  final int id;
  const Model94(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model95 {
  final int id;
  const Model95(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model96 {
  final int id;
  const Model96(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model97 {
  final int id;
  const Model97(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model98 {
  final int id;
  const Model98(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model99 {
  final int id;
  const Model99(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model100 {
  final int id;
  const Model100(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model101 {
  final int id;
  const Model101(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model102 {
  final int id;
  const Model102(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model103 {
  final int id;
  const Model103(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model104 {
  final int id;
  const Model104(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model105 {
  final int id;
  const Model105(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model106 {
  final int id;
  const Model106(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model107 {
  final int id;
  const Model107(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model108 {
  final int id;
  const Model108(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model109 {
  final int id;
  const Model109(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model110 {
  final int id;
  const Model110(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model111 {
  final int id;
  const Model111(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model112 {
  final int id;
  const Model112(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model113 {
  final int id;
  const Model113(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model114 {
  final int id;
  const Model114(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model115 {
  final int id;
  const Model115(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model116 {
  final int id;
  const Model116(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model117 {
  final int id;
  const Model117(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model118 {
  final int id;
  const Model118(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model119 {
  final int id;
  const Model119(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model120 {
  final int id;
  const Model120(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model121 {
  final int id;
  const Model121(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model122 {
  final int id;
  const Model122(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model123 {
  final int id;
  const Model123(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model124 {
  final int id;
  const Model124(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model125 {
  final int id;
  const Model125(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model126 {
  final int id;
  const Model126(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model127 {
  final int id;
  const Model127(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model128 {
  final int id;
  const Model128(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model129 {
  final int id;
  const Model129(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model130 {
  final int id;
  const Model130(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model131 {
  final int id;
  const Model131(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model132 {
  final int id;
  const Model132(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model133 {
  final int id;
  const Model133(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model134 {
  final int id;
  const Model134(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model135 {
  final int id;
  const Model135(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model136 {
  final int id;
  const Model136(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model137 {
  final int id;
  const Model137(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model138 {
  final int id;
  const Model138(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model139 {
  final int id;
  const Model139(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model140 {
  final int id;
  const Model140(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model141 {
  final int id;
  const Model141(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model142 {
  final int id;
  const Model142(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model143 {
  final int id;
  const Model143(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model144 {
  final int id;
  const Model144(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model145 {
  final int id;
  const Model145(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model146 {
  final int id;
  const Model146(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model147 {
  final int id;
  const Model147(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model148 {
  final int id;
  const Model148(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model149 {
  final int id;
  const Model149(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model150 {
  final int id;
  const Model150(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model151 {
  final int id;
  const Model151(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model152 {
  final int id;
  const Model152(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model153 {
  final int id;
  const Model153(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model154 {
  final int id;
  const Model154(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model155 {
  final int id;
  const Model155(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model156 {
  final int id;
  const Model156(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model157 {
  final int id;
  const Model157(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model158 {
  final int id;
  const Model158(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model159 {
  final int id;
  const Model159(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model160 {
  final int id;
  const Model160(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model161 {
  final int id;
  const Model161(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model162 {
  final int id;
  const Model162(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model163 {
  final int id;
  const Model163(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model164 {
  final int id;
  const Model164(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model165 {
  final int id;
  const Model165(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model166 {
  final int id;
  const Model166(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model167 {
  final int id;
  const Model167(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model168 {
  final int id;
  const Model168(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model169 {
  final int id;
  const Model169(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model170 {
  final int id;
  const Model170(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model171 {
  final int id;
  const Model171(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model172 {
  final int id;
  const Model172(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model173 {
  final int id;
  const Model173(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model174 {
  final int id;
  const Model174(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model175 {
  final int id;
  const Model175(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model176 {
  final int id;
  const Model176(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model177 {
  final int id;
  const Model177(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model178 {
  final int id;
  const Model178(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model179 {
  final int id;
  const Model179(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model180 {
  final int id;
  const Model180(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model181 {
  final int id;
  const Model181(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model182 {
  final int id;
  const Model182(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model183 {
  final int id;
  const Model183(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model184 {
  final int id;
  const Model184(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model185 {
  final int id;
  const Model185(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model186 {
  final int id;
  const Model186(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model187 {
  final int id;
  const Model187(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model188 {
  final int id;
  const Model188(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model189 {
  final int id;
  const Model189(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model190 {
  final int id;
  const Model190(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model191 {
  final int id;
  const Model191(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model192 {
  final int id;
  const Model192(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model193 {
  final int id;
  const Model193(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model194 {
  final int id;
  const Model194(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model195 {
  final int id;
  const Model195(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model196 {
  final int id;
  const Model196(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model197 {
  final int id;
  const Model197(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model198 {
  final int id;
  const Model198(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model199 {
  final int id;
  const Model199(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model200 {
  final int id;
  const Model200(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model201 {
  final int id;
  const Model201(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model202 {
  final int id;
  const Model202(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model203 {
  final int id;
  const Model203(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model204 {
  final int id;
  const Model204(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model205 {
  final int id;
  const Model205(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model206 {
  final int id;
  const Model206(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model207 {
  final int id;
  const Model207(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model208 {
  final int id;
  const Model208(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model209 {
  final int id;
  const Model209(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model210 {
  final int id;
  const Model210(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model211 {
  final int id;
  const Model211(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model212 {
  final int id;
  const Model212(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model213 {
  final int id;
  const Model213(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model214 {
  final int id;
  const Model214(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model215 {
  final int id;
  const Model215(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model216 {
  final int id;
  const Model216(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model217 {
  final int id;
  const Model217(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model218 {
  final int id;
  const Model218(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model219 {
  final int id;
  const Model219(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model220 {
  final int id;
  const Model220(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model221 {
  final int id;
  const Model221(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model222 {
  final int id;
  const Model222(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model223 {
  final int id;
  const Model223(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model224 {
  final int id;
  const Model224(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model225 {
  final int id;
  const Model225(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model226 {
  final int id;
  const Model226(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model227 {
  final int id;
  const Model227(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model228 {
  final int id;
  const Model228(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model229 {
  final int id;
  const Model229(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model230 {
  final int id;
  const Model230(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model231 {
  final int id;
  const Model231(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model232 {
  final int id;
  const Model232(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model233 {
  final int id;
  const Model233(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model234 {
  final int id;
  const Model234(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model235 {
  final int id;
  const Model235(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model236 {
  final int id;
  const Model236(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model237 {
  final int id;
  const Model237(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model238 {
  final int id;
  const Model238(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model239 {
  final int id;
  const Model239(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model240 {
  final int id;
  const Model240(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model241 {
  final int id;
  const Model241(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model242 {
  final int id;
  const Model242(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model243 {
  final int id;
  const Model243(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model244 {
  final int id;
  const Model244(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model245 {
  final int id;
  const Model245(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model246 {
  final int id;
  const Model246(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model247 {
  final int id;
  const Model247(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model248 {
  final int id;
  const Model248(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model249 {
  final int id;
  const Model249(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model250 {
  final int id;
  const Model250(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model251 {
  final int id;
  const Model251(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model252 {
  final int id;
  const Model252(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model253 {
  final int id;
  const Model253(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model254 {
  final int id;
  const Model254(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model255 {
  final int id;
  const Model255(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model256 {
  final int id;
  const Model256(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model257 {
  final int id;
  const Model257(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model258 {
  final int id;
  const Model258(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model259 {
  final int id;
  const Model259(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model260 {
  final int id;
  const Model260(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model261 {
  final int id;
  const Model261(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model262 {
  final int id;
  const Model262(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model263 {
  final int id;
  const Model263(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model264 {
  final int id;
  const Model264(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model265 {
  final int id;
  const Model265(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model266 {
  final int id;
  const Model266(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model267 {
  final int id;
  const Model267(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model268 {
  final int id;
  const Model268(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model269 {
  final int id;
  const Model269(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model270 {
  final int id;
  const Model270(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model271 {
  final int id;
  const Model271(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model272 {
  final int id;
  const Model272(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model273 {
  final int id;
  const Model273(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model274 {
  final int id;
  const Model274(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model275 {
  final int id;
  const Model275(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model276 {
  final int id;
  const Model276(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model277 {
  final int id;
  const Model277(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model278 {
  final int id;
  const Model278(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model279 {
  final int id;
  const Model279(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model280 {
  final int id;
  const Model280(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model281 {
  final int id;
  const Model281(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model282 {
  final int id;
  const Model282(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model283 {
  final int id;
  const Model283(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model284 {
  final int id;
  const Model284(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model285 {
  final int id;
  const Model285(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model286 {
  final int id;
  const Model286(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model287 {
  final int id;
  const Model287(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model288 {
  final int id;
  const Model288(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model289 {
  final int id;
  const Model289(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model290 {
  final int id;
  const Model290(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model291 {
  final int id;
  const Model291(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model292 {
  final int id;
  const Model292(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model293 {
  final int id;
  const Model293(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model294 {
  final int id;
  const Model294(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model295 {
  final int id;
  const Model295(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model296 {
  final int id;
  const Model296(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model297 {
  final int id;
  const Model297(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model298 {
  final int id;
  const Model298(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model299 {
  final int id;
  const Model299(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model300 {
  final int id;
  const Model300(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model301 {
  final int id;
  const Model301(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model302 {
  final int id;
  const Model302(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model303 {
  final int id;
  const Model303(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model304 {
  final int id;
  const Model304(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model305 {
  final int id;
  const Model305(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model306 {
  final int id;
  const Model306(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model307 {
  final int id;
  const Model307(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model308 {
  final int id;
  const Model308(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model309 {
  final int id;
  const Model309(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model310 {
  final int id;
  const Model310(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model311 {
  final int id;
  const Model311(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model312 {
  final int id;
  const Model312(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model313 {
  final int id;
  const Model313(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model314 {
  final int id;
  const Model314(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model315 {
  final int id;
  const Model315(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model316 {
  final int id;
  const Model316(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model317 {
  final int id;
  const Model317(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model318 {
  final int id;
  const Model318(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model319 {
  final int id;
  const Model319(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model320 {
  final int id;
  const Model320(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model321 {
  final int id;
  const Model321(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model322 {
  final int id;
  const Model322(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model323 {
  final int id;
  const Model323(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model324 {
  final int id;
  const Model324(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model325 {
  final int id;
  const Model325(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model326 {
  final int id;
  const Model326(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model327 {
  final int id;
  const Model327(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model328 {
  final int id;
  const Model328(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model329 {
  final int id;
  const Model329(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model330 {
  final int id;
  const Model330(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model331 {
  final int id;
  const Model331(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model332 {
  final int id;
  const Model332(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model333 {
  final int id;
  const Model333(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model334 {
  final int id;
  const Model334(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model335 {
  final int id;
  const Model335(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model336 {
  final int id;
  const Model336(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model337 {
  final int id;
  const Model337(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model338 {
  final int id;
  const Model338(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model339 {
  final int id;
  const Model339(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model340 {
  final int id;
  const Model340(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model341 {
  final int id;
  const Model341(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model342 {
  final int id;
  const Model342(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model343 {
  final int id;
  const Model343(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model344 {
  final int id;
  const Model344(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model345 {
  final int id;
  const Model345(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model346 {
  final int id;
  const Model346(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model347 {
  final int id;
  const Model347(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model348 {
  final int id;
  const Model348(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model349 {
  final int id;
  const Model349(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model350 {
  final int id;
  const Model350(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model351 {
  final int id;
  const Model351(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model352 {
  final int id;
  const Model352(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model353 {
  final int id;
  const Model353(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model354 {
  final int id;
  const Model354(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model355 {
  final int id;
  const Model355(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model356 {
  final int id;
  const Model356(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model357 {
  final int id;
  const Model357(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model358 {
  final int id;
  const Model358(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model359 {
  final int id;
  const Model359(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model360 {
  final int id;
  const Model360(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model361 {
  final int id;
  const Model361(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model362 {
  final int id;
  const Model362(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model363 {
  final int id;
  const Model363(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model364 {
  final int id;
  const Model364(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model365 {
  final int id;
  const Model365(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model366 {
  final int id;
  const Model366(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model367 {
  final int id;
  const Model367(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model368 {
  final int id;
  const Model368(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model369 {
  final int id;
  const Model369(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model370 {
  final int id;
  const Model370(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model371 {
  final int id;
  const Model371(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model372 {
  final int id;
  const Model372(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model373 {
  final int id;
  const Model373(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model374 {
  final int id;
  const Model374(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model375 {
  final int id;
  const Model375(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model376 {
  final int id;
  const Model376(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model377 {
  final int id;
  const Model377(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model378 {
  final int id;
  const Model378(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model379 {
  final int id;
  const Model379(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model380 {
  final int id;
  const Model380(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model381 {
  final int id;
  const Model381(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model382 {
  final int id;
  const Model382(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model383 {
  final int id;
  const Model383(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model384 {
  final int id;
  const Model384(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model385 {
  final int id;
  const Model385(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model386 {
  final int id;
  const Model386(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model387 {
  final int id;
  const Model387(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model388 {
  final int id;
  const Model388(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model389 {
  final int id;
  const Model389(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model390 {
  final int id;
  const Model390(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model391 {
  final int id;
  const Model391(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model392 {
  final int id;
  const Model392(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model393 {
  final int id;
  const Model393(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model394 {
  final int id;
  const Model394(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model395 {
  final int id;
  const Model395(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model396 {
  final int id;
  const Model396(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model397 {
  final int id;
  const Model397(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model398 {
  final int id;
  const Model398(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model399 {
  final int id;
  const Model399(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model400 {
  final int id;
  const Model400(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model401 {
  final int id;
  const Model401(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model402 {
  final int id;
  const Model402(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model403 {
  final int id;
  const Model403(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model404 {
  final int id;
  const Model404(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model405 {
  final int id;
  const Model405(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model406 {
  final int id;
  const Model406(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model407 {
  final int id;
  const Model407(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model408 {
  final int id;
  const Model408(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model409 {
  final int id;
  const Model409(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model410 {
  final int id;
  const Model410(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model411 {
  final int id;
  const Model411(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model412 {
  final int id;
  const Model412(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model413 {
  final int id;
  const Model413(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model414 {
  final int id;
  const Model414(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model415 {
  final int id;
  const Model415(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model416 {
  final int id;
  const Model416(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model417 {
  final int id;
  const Model417(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model418 {
  final int id;
  const Model418(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model419 {
  final int id;
  const Model419(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model420 {
  final int id;
  const Model420(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model421 {
  final int id;
  const Model421(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model422 {
  final int id;
  const Model422(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model423 {
  final int id;
  const Model423(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model424 {
  final int id;
  const Model424(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model425 {
  final int id;
  const Model425(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model426 {
  final int id;
  const Model426(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model427 {
  final int id;
  const Model427(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model428 {
  final int id;
  const Model428(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model429 {
  final int id;
  const Model429(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model430 {
  final int id;
  const Model430(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model431 {
  final int id;
  const Model431(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model432 {
  final int id;
  const Model432(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model433 {
  final int id;
  const Model433(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model434 {
  final int id;
  const Model434(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model435 {
  final int id;
  const Model435(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model436 {
  final int id;
  const Model436(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model437 {
  final int id;
  const Model437(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model438 {
  final int id;
  const Model438(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model439 {
  final int id;
  const Model439(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model440 {
  final int id;
  const Model440(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model441 {
  final int id;
  const Model441(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model442 {
  final int id;
  const Model442(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model443 {
  final int id;
  const Model443(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model444 {
  final int id;
  const Model444(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model445 {
  final int id;
  const Model445(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model446 {
  final int id;
  const Model446(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model447 {
  final int id;
  const Model447(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model448 {
  final int id;
  const Model448(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model449 {
  final int id;
  const Model449(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model450 {
  final int id;
  const Model450(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model451 {
  final int id;
  const Model451(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model452 {
  final int id;
  const Model452(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model453 {
  final int id;
  const Model453(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model454 {
  final int id;
  const Model454(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model455 {
  final int id;
  const Model455(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model456 {
  final int id;
  const Model456(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model457 {
  final int id;
  const Model457(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model458 {
  final int id;
  const Model458(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model459 {
  final int id;
  const Model459(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model460 {
  final int id;
  const Model460(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model461 {
  final int id;
  const Model461(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model462 {
  final int id;
  const Model462(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model463 {
  final int id;
  const Model463(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model464 {
  final int id;
  const Model464(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model465 {
  final int id;
  const Model465(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model466 {
  final int id;
  const Model466(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model467 {
  final int id;
  const Model467(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model468 {
  final int id;
  const Model468(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model469 {
  final int id;
  const Model469(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model470 {
  final int id;
  const Model470(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model471 {
  final int id;
  const Model471(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model472 {
  final int id;
  const Model472(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model473 {
  final int id;
  const Model473(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model474 {
  final int id;
  const Model474(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model475 {
  final int id;
  const Model475(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model476 {
  final int id;
  const Model476(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model477 {
  final int id;
  const Model477(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model478 {
  final int id;
  const Model478(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model479 {
  final int id;
  const Model479(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model480 {
  final int id;
  const Model480(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model481 {
  final int id;
  const Model481(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model482 {
  final int id;
  const Model482(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model483 {
  final int id;
  const Model483(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model484 {
  final int id;
  const Model484(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model485 {
  final int id;
  const Model485(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model486 {
  final int id;
  const Model486(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model487 {
  final int id;
  const Model487(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model488 {
  final int id;
  const Model488(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model489 {
  final int id;
  const Model489(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model490 {
  final int id;
  const Model490(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model491 {
  final int id;
  const Model491(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model492 {
  final int id;
  const Model492(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model493 {
  final int id;
  const Model493(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model494 {
  final int id;
  const Model494(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model495 {
  final int id;
  const Model495(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model496 {
  final int id;
  const Model496(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model497 {
  final int id;
  const Model497(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model498 {
  final int id;
  const Model498(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model499 {
  final int id;
  const Model499(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model500 {
  final int id;
  const Model500(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model501 {
  final int id;
  const Model501(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model502 {
  final int id;
  const Model502(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model503 {
  final int id;
  const Model503(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model504 {
  final int id;
  const Model504(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model505 {
  final int id;
  const Model505(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model506 {
  final int id;
  const Model506(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model507 {
  final int id;
  const Model507(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model508 {
  final int id;
  const Model508(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model509 {
  final int id;
  const Model509(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model510 {
  final int id;
  const Model510(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model511 {
  final int id;
  const Model511(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model512 {
  final int id;
  const Model512(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model513 {
  final int id;
  const Model513(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model514 {
  final int id;
  const Model514(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model515 {
  final int id;
  const Model515(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model516 {
  final int id;
  const Model516(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model517 {
  final int id;
  const Model517(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model518 {
  final int id;
  const Model518(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model519 {
  final int id;
  const Model519(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model520 {
  final int id;
  const Model520(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model521 {
  final int id;
  const Model521(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model522 {
  final int id;
  const Model522(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model523 {
  final int id;
  const Model523(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model524 {
  final int id;
  const Model524(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model525 {
  final int id;
  const Model525(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model526 {
  final int id;
  const Model526(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model527 {
  final int id;
  const Model527(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model528 {
  final int id;
  const Model528(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model529 {
  final int id;
  const Model529(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model530 {
  final int id;
  const Model530(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model531 {
  final int id;
  const Model531(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model532 {
  final int id;
  const Model532(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model533 {
  final int id;
  const Model533(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model534 {
  final int id;
  const Model534(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model535 {
  final int id;
  const Model535(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model536 {
  final int id;
  const Model536(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model537 {
  final int id;
  const Model537(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model538 {
  final int id;
  const Model538(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model539 {
  final int id;
  const Model539(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model540 {
  final int id;
  const Model540(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model541 {
  final int id;
  const Model541(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model542 {
  final int id;
  const Model542(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model543 {
  final int id;
  const Model543(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model544 {
  final int id;
  const Model544(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model545 {
  final int id;
  const Model545(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model546 {
  final int id;
  const Model546(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model547 {
  final int id;
  const Model547(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model548 {
  final int id;
  const Model548(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model549 {
  final int id;
  const Model549(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model550 {
  final int id;
  const Model550(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model551 {
  final int id;
  const Model551(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model552 {
  final int id;
  const Model552(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model553 {
  final int id;
  const Model553(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model554 {
  final int id;
  const Model554(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model555 {
  final int id;
  const Model555(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model556 {
  final int id;
  const Model556(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model557 {
  final int id;
  const Model557(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model558 {
  final int id;
  const Model558(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model559 {
  final int id;
  const Model559(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model560 {
  final int id;
  const Model560(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model561 {
  final int id;
  const Model561(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model562 {
  final int id;
  const Model562(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model563 {
  final int id;
  const Model563(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model564 {
  final int id;
  const Model564(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model565 {
  final int id;
  const Model565(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model566 {
  final int id;
  const Model566(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model567 {
  final int id;
  const Model567(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model568 {
  final int id;
  const Model568(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model569 {
  final int id;
  const Model569(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model570 {
  final int id;
  const Model570(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model571 {
  final int id;
  const Model571(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model572 {
  final int id;
  const Model572(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model573 {
  final int id;
  const Model573(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model574 {
  final int id;
  const Model574(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model575 {
  final int id;
  const Model575(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model576 {
  final int id;
  const Model576(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model577 {
  final int id;
  const Model577(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model578 {
  final int id;
  const Model578(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model579 {
  final int id;
  const Model579(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model580 {
  final int id;
  const Model580(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model581 {
  final int id;
  const Model581(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model582 {
  final int id;
  const Model582(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model583 {
  final int id;
  const Model583(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model584 {
  final int id;
  const Model584(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model585 {
  final int id;
  const Model585(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model586 {
  final int id;
  const Model586(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model587 {
  final int id;
  const Model587(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model588 {
  final int id;
  const Model588(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model589 {
  final int id;
  const Model589(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model590 {
  final int id;
  const Model590(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model591 {
  final int id;
  const Model591(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model592 {
  final int id;
  const Model592(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model593 {
  final int id;
  const Model593(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model594 {
  final int id;
  const Model594(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model595 {
  final int id;
  const Model595(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model596 {
  final int id;
  const Model596(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model597 {
  final int id;
  const Model597(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model598 {
  final int id;
  const Model598(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model599 {
  final int id;
  const Model599(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model600 {
  final int id;
  const Model600(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model601 {
  final int id;
  const Model601(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model602 {
  final int id;
  const Model602(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model603 {
  final int id;
  const Model603(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model604 {
  final int id;
  const Model604(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model605 {
  final int id;
  const Model605(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model606 {
  final int id;
  const Model606(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model607 {
  final int id;
  const Model607(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model608 {
  final int id;
  const Model608(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model609 {
  final int id;
  const Model609(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model610 {
  final int id;
  const Model610(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model611 {
  final int id;
  const Model611(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model612 {
  final int id;
  const Model612(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model613 {
  final int id;
  const Model613(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model614 {
  final int id;
  const Model614(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model615 {
  final int id;
  const Model615(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model616 {
  final int id;
  const Model616(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model617 {
  final int id;
  const Model617(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model618 {
  final int id;
  const Model618(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model619 {
  final int id;
  const Model619(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model620 {
  final int id;
  const Model620(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model621 {
  final int id;
  const Model621(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model622 {
  final int id;
  const Model622(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model623 {
  final int id;
  const Model623(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model624 {
  final int id;
  const Model624(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model625 {
  final int id;
  const Model625(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model626 {
  final int id;
  const Model626(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model627 {
  final int id;
  const Model627(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model628 {
  final int id;
  const Model628(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model629 {
  final int id;
  const Model629(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model630 {
  final int id;
  const Model630(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model631 {
  final int id;
  const Model631(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model632 {
  final int id;
  const Model632(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model633 {
  final int id;
  const Model633(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model634 {
  final int id;
  const Model634(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model635 {
  final int id;
  const Model635(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model636 {
  final int id;
  const Model636(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model637 {
  final int id;
  const Model637(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model638 {
  final int id;
  const Model638(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model639 {
  final int id;
  const Model639(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model640 {
  final int id;
  const Model640(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model641 {
  final int id;
  const Model641(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model642 {
  final int id;
  const Model642(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model643 {
  final int id;
  const Model643(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model644 {
  final int id;
  const Model644(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model645 {
  final int id;
  const Model645(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model646 {
  final int id;
  const Model646(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model647 {
  final int id;
  const Model647(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model648 {
  final int id;
  const Model648(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model649 {
  final int id;
  const Model649(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model650 {
  final int id;
  const Model650(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model651 {
  final int id;
  const Model651(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model652 {
  final int id;
  const Model652(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model653 {
  final int id;
  const Model653(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model654 {
  final int id;
  const Model654(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model655 {
  final int id;
  const Model655(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model656 {
  final int id;
  const Model656(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model657 {
  final int id;
  const Model657(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model658 {
  final int id;
  const Model658(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model659 {
  final int id;
  const Model659(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model660 {
  final int id;
  const Model660(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model661 {
  final int id;
  const Model661(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model662 {
  final int id;
  const Model662(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model663 {
  final int id;
  const Model663(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model664 {
  final int id;
  const Model664(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model665 {
  final int id;
  const Model665(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model666 {
  final int id;
  const Model666(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model667 {
  final int id;
  const Model667(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model668 {
  final int id;
  const Model668(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model669 {
  final int id;
  const Model669(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model670 {
  final int id;
  const Model670(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model671 {
  final int id;
  const Model671(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model672 {
  final int id;
  const Model672(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model673 {
  final int id;
  const Model673(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model674 {
  final int id;
  const Model674(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model675 {
  final int id;
  const Model675(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model676 {
  final int id;
  const Model676(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model677 {
  final int id;
  const Model677(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model678 {
  final int id;
  const Model678(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model679 {
  final int id;
  const Model679(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model680 {
  final int id;
  const Model680(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model681 {
  final int id;
  const Model681(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model682 {
  final int id;
  const Model682(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model683 {
  final int id;
  const Model683(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model684 {
  final int id;
  const Model684(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model685 {
  final int id;
  const Model685(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model686 {
  final int id;
  const Model686(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model687 {
  final int id;
  const Model687(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model688 {
  final int id;
  const Model688(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model689 {
  final int id;
  const Model689(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model690 {
  final int id;
  const Model690(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model691 {
  final int id;
  const Model691(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model692 {
  final int id;
  const Model692(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model693 {
  final int id;
  const Model693(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model694 {
  final int id;
  const Model694(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model695 {
  final int id;
  const Model695(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model696 {
  final int id;
  const Model696(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model697 {
  final int id;
  const Model697(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model698 {
  final int id;
  const Model698(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model699 {
  final int id;
  const Model699(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model700 {
  final int id;
  const Model700(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model701 {
  final int id;
  const Model701(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model702 {
  final int id;
  const Model702(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model703 {
  final int id;
  const Model703(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model704 {
  final int id;
  const Model704(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model705 {
  final int id;
  const Model705(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model706 {
  final int id;
  const Model706(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model707 {
  final int id;
  const Model707(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model708 {
  final int id;
  const Model708(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model709 {
  final int id;
  const Model709(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model710 {
  final int id;
  const Model710(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model711 {
  final int id;
  const Model711(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model712 {
  final int id;
  const Model712(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model713 {
  final int id;
  const Model713(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model714 {
  final int id;
  const Model714(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model715 {
  final int id;
  const Model715(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model716 {
  final int id;
  const Model716(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model717 {
  final int id;
  const Model717(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model718 {
  final int id;
  const Model718(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model719 {
  final int id;
  const Model719(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model720 {
  final int id;
  const Model720(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model721 {
  final int id;
  const Model721(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model722 {
  final int id;
  const Model722(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model723 {
  final int id;
  const Model723(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model724 {
  final int id;
  const Model724(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model725 {
  final int id;
  const Model725(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model726 {
  final int id;
  const Model726(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model727 {
  final int id;
  const Model727(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model728 {
  final int id;
  const Model728(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model729 {
  final int id;
  const Model729(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model730 {
  final int id;
  const Model730(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model731 {
  final int id;
  const Model731(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model732 {
  final int id;
  const Model732(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model733 {
  final int id;
  const Model733(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model734 {
  final int id;
  const Model734(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model735 {
  final int id;
  const Model735(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model736 {
  final int id;
  const Model736(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model737 {
  final int id;
  const Model737(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model738 {
  final int id;
  const Model738(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model739 {
  final int id;
  const Model739(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model740 {
  final int id;
  const Model740(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model741 {
  final int id;
  const Model741(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model742 {
  final int id;
  const Model742(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model743 {
  final int id;
  const Model743(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model744 {
  final int id;
  const Model744(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model745 {
  final int id;
  const Model745(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model746 {
  final int id;
  const Model746(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model747 {
  final int id;
  const Model747(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model748 {
  final int id;
  const Model748(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model749 {
  final int id;
  const Model749(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model750 {
  final int id;
  const Model750(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model751 {
  final int id;
  const Model751(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model752 {
  final int id;
  const Model752(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model753 {
  final int id;
  const Model753(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model754 {
  final int id;
  const Model754(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model755 {
  final int id;
  const Model755(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model756 {
  final int id;
  const Model756(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model757 {
  final int id;
  const Model757(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model758 {
  final int id;
  const Model758(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model759 {
  final int id;
  const Model759(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model760 {
  final int id;
  const Model760(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model761 {
  final int id;
  const Model761(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model762 {
  final int id;
  const Model762(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model763 {
  final int id;
  const Model763(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model764 {
  final int id;
  const Model764(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model765 {
  final int id;
  const Model765(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model766 {
  final int id;
  const Model766(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model767 {
  final int id;
  const Model767(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model768 {
  final int id;
  const Model768(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model769 {
  final int id;
  const Model769(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model770 {
  final int id;
  const Model770(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model771 {
  final int id;
  const Model771(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model772 {
  final int id;
  const Model772(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model773 {
  final int id;
  const Model773(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model774 {
  final int id;
  const Model774(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model775 {
  final int id;
  const Model775(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model776 {
  final int id;
  const Model776(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model777 {
  final int id;
  const Model777(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model778 {
  final int id;
  const Model778(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model779 {
  final int id;
  const Model779(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model780 {
  final int id;
  const Model780(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model781 {
  final int id;
  const Model781(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model782 {
  final int id;
  const Model782(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model783 {
  final int id;
  const Model783(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model784 {
  final int id;
  const Model784(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model785 {
  final int id;
  const Model785(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model786 {
  final int id;
  const Model786(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model787 {
  final int id;
  const Model787(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model788 {
  final int id;
  const Model788(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model789 {
  final int id;
  const Model789(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model790 {
  final int id;
  const Model790(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model791 {
  final int id;
  const Model791(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model792 {
  final int id;
  const Model792(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model793 {
  final int id;
  const Model793(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model794 {
  final int id;
  const Model794(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model795 {
  final int id;
  const Model795(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model796 {
  final int id;
  const Model796(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model797 {
  final int id;
  const Model797(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model798 {
  final int id;
  const Model798(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model799 {
  final int id;
  const Model799(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model800 {
  final int id;
  const Model800(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model801 {
  final int id;
  const Model801(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model802 {
  final int id;
  const Model802(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model803 {
  final int id;
  const Model803(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model804 {
  final int id;
  const Model804(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model805 {
  final int id;
  const Model805(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model806 {
  final int id;
  const Model806(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model807 {
  final int id;
  const Model807(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model808 {
  final int id;
  const Model808(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model809 {
  final int id;
  const Model809(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model810 {
  final int id;
  const Model810(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model811 {
  final int id;
  const Model811(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model812 {
  final int id;
  const Model812(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model813 {
  final int id;
  const Model813(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model814 {
  final int id;
  const Model814(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model815 {
  final int id;
  const Model815(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model816 {
  final int id;
  const Model816(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model817 {
  final int id;
  const Model817(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model818 {
  final int id;
  const Model818(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model819 {
  final int id;
  const Model819(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model820 {
  final int id;
  const Model820(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model821 {
  final int id;
  const Model821(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model822 {
  final int id;
  const Model822(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model823 {
  final int id;
  const Model823(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model824 {
  final int id;
  const Model824(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model825 {
  final int id;
  const Model825(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model826 {
  final int id;
  const Model826(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model827 {
  final int id;
  const Model827(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model828 {
  final int id;
  const Model828(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model829 {
  final int id;
  const Model829(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model830 {
  final int id;
  const Model830(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model831 {
  final int id;
  const Model831(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model832 {
  final int id;
  const Model832(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model833 {
  final int id;
  const Model833(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model834 {
  final int id;
  const Model834(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model835 {
  final int id;
  const Model835(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model836 {
  final int id;
  const Model836(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model837 {
  final int id;
  const Model837(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model838 {
  final int id;
  const Model838(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model839 {
  final int id;
  const Model839(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model840 {
  final int id;
  const Model840(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model841 {
  final int id;
  const Model841(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model842 {
  final int id;
  const Model842(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model843 {
  final int id;
  const Model843(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model844 {
  final int id;
  const Model844(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model845 {
  final int id;
  const Model845(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model846 {
  final int id;
  const Model846(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model847 {
  final int id;
  const Model847(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model848 {
  final int id;
  const Model848(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model849 {
  final int id;
  const Model849(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model850 {
  final int id;
  const Model850(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model851 {
  final int id;
  const Model851(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model852 {
  final int id;
  const Model852(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model853 {
  final int id;
  const Model853(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model854 {
  final int id;
  const Model854(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model855 {
  final int id;
  const Model855(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model856 {
  final int id;
  const Model856(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model857 {
  final int id;
  const Model857(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model858 {
  final int id;
  const Model858(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model859 {
  final int id;
  const Model859(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model860 {
  final int id;
  const Model860(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model861 {
  final int id;
  const Model861(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model862 {
  final int id;
  const Model862(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model863 {
  final int id;
  const Model863(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model864 {
  final int id;
  const Model864(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model865 {
  final int id;
  const Model865(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model866 {
  final int id;
  const Model866(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model867 {
  final int id;
  const Model867(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model868 {
  final int id;
  const Model868(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model869 {
  final int id;
  const Model869(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model870 {
  final int id;
  const Model870(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model871 {
  final int id;
  const Model871(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model872 {
  final int id;
  const Model872(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model873 {
  final int id;
  const Model873(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model874 {
  final int id;
  const Model874(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model875 {
  final int id;
  const Model875(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model876 {
  final int id;
  const Model876(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model877 {
  final int id;
  const Model877(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model878 {
  final int id;
  const Model878(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model879 {
  final int id;
  const Model879(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model880 {
  final int id;
  const Model880(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model881 {
  final int id;
  const Model881(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model882 {
  final int id;
  const Model882(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model883 {
  final int id;
  const Model883(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model884 {
  final int id;
  const Model884(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model885 {
  final int id;
  const Model885(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model886 {
  final int id;
  const Model886(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model887 {
  final int id;
  const Model887(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model888 {
  final int id;
  const Model888(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model889 {
  final int id;
  const Model889(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model890 {
  final int id;
  const Model890(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model891 {
  final int id;
  const Model891(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model892 {
  final int id;
  const Model892(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model893 {
  final int id;
  const Model893(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model894 {
  final int id;
  const Model894(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model895 {
  final int id;
  const Model895(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model896 {
  final int id;
  const Model896(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model897 {
  final int id;
  const Model897(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model898 {
  final int id;
  const Model898(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model899 {
  final int id;
  const Model899(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model900 {
  final int id;
  const Model900(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model901 {
  final int id;
  const Model901(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model902 {
  final int id;
  const Model902(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model903 {
  final int id;
  const Model903(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model904 {
  final int id;
  const Model904(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model905 {
  final int id;
  const Model905(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model906 {
  final int id;
  const Model906(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model907 {
  final int id;
  const Model907(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model908 {
  final int id;
  const Model908(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model909 {
  final int id;
  const Model909(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model910 {
  final int id;
  const Model910(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model911 {
  final int id;
  const Model911(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model912 {
  final int id;
  const Model912(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model913 {
  final int id;
  const Model913(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model914 {
  final int id;
  const Model914(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model915 {
  final int id;
  const Model915(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model916 {
  final int id;
  const Model916(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model917 {
  final int id;
  const Model917(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model918 {
  final int id;
  const Model918(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model919 {
  final int id;
  const Model919(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model920 {
  final int id;
  const Model920(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model921 {
  final int id;
  const Model921(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model922 {
  final int id;
  const Model922(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model923 {
  final int id;
  const Model923(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model924 {
  final int id;
  const Model924(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model925 {
  final int id;
  const Model925(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model926 {
  final int id;
  const Model926(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model927 {
  final int id;
  const Model927(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model928 {
  final int id;
  const Model928(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model929 {
  final int id;
  const Model929(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model930 {
  final int id;
  const Model930(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model931 {
  final int id;
  const Model931(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model932 {
  final int id;
  const Model932(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model933 {
  final int id;
  const Model933(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model934 {
  final int id;
  const Model934(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model935 {
  final int id;
  const Model935(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model936 {
  final int id;
  const Model936(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model937 {
  final int id;
  const Model937(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model938 {
  final int id;
  const Model938(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model939 {
  final int id;
  const Model939(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model940 {
  final int id;
  const Model940(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model941 {
  final int id;
  const Model941(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model942 {
  final int id;
  const Model942(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model943 {
  final int id;
  const Model943(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model944 {
  final int id;
  const Model944(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model945 {
  final int id;
  const Model945(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model946 {
  final int id;
  const Model946(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model947 {
  final int id;
  const Model947(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model948 {
  final int id;
  const Model948(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model949 {
  final int id;
  const Model949(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model950 {
  final int id;
  const Model950(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model951 {
  final int id;
  const Model951(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model952 {
  final int id;
  const Model952(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model953 {
  final int id;
  const Model953(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model954 {
  final int id;
  const Model954(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model955 {
  final int id;
  const Model955(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model956 {
  final int id;
  const Model956(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model957 {
  final int id;
  const Model957(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model958 {
  final int id;
  const Model958(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model959 {
  final int id;
  const Model959(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model960 {
  final int id;
  const Model960(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model961 {
  final int id;
  const Model961(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model962 {
  final int id;
  const Model962(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model963 {
  final int id;
  const Model963(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model964 {
  final int id;
  const Model964(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model965 {
  final int id;
  const Model965(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model966 {
  final int id;
  const Model966(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model967 {
  final int id;
  const Model967(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model968 {
  final int id;
  const Model968(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model969 {
  final int id;
  const Model969(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model970 {
  final int id;
  const Model970(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model971 {
  final int id;
  const Model971(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model972 {
  final int id;
  const Model972(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model973 {
  final int id;
  const Model973(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model974 {
  final int id;
  const Model974(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model975 {
  final int id;
  const Model975(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model976 {
  final int id;
  const Model976(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model977 {
  final int id;
  const Model977(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model978 {
  final int id;
  const Model978(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model979 {
  final int id;
  const Model979(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model980 {
  final int id;
  const Model980(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model981 {
  final int id;
  const Model981(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model982 {
  final int id;
  const Model982(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model983 {
  final int id;
  const Model983(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model984 {
  final int id;
  const Model984(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model985 {
  final int id;
  const Model985(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model986 {
  final int id;
  const Model986(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model987 {
  final int id;
  const Model987(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model988 {
  final int id;
  const Model988(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model989 {
  final int id;
  const Model989(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model990 {
  final int id;
  const Model990(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model991 {
  final int id;
  const Model991(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model992 {
  final int id;
  const Model992(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model993 {
  final int id;
  const Model993(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model994 {
  final int id;
  const Model994(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model995 {
  final int id;
  const Model995(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model996 {
  final int id;
  const Model996(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model997 {
  final int id;
  const Model997(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model998 {
  final int id;
  const Model998(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model999 {
  final int id;
  const Model999(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1000 {
  final int id;
  const Model1000(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1001 {
  final int id;
  const Model1001(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1002 {
  final int id;
  const Model1002(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1003 {
  final int id;
  const Model1003(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1004 {
  final int id;
  const Model1004(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1005 {
  final int id;
  const Model1005(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1006 {
  final int id;
  const Model1006(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1007 {
  final int id;
  const Model1007(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1008 {
  final int id;
  const Model1008(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1009 {
  final int id;
  const Model1009(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1010 {
  final int id;
  const Model1010(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1011 {
  final int id;
  const Model1011(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1012 {
  final int id;
  const Model1012(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1013 {
  final int id;
  const Model1013(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1014 {
  final int id;
  const Model1014(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1015 {
  final int id;
  const Model1015(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1016 {
  final int id;
  const Model1016(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1017 {
  final int id;
  const Model1017(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1018 {
  final int id;
  const Model1018(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1019 {
  final int id;
  const Model1019(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1020 {
  final int id;
  const Model1020(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1021 {
  final int id;
  const Model1021(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1022 {
  final int id;
  const Model1022(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1023 {
  final int id;
  const Model1023(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1024 {
  final int id;
  const Model1024(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1025 {
  final int id;
  const Model1025(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1026 {
  final int id;
  const Model1026(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1027 {
  final int id;
  const Model1027(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1028 {
  final int id;
  const Model1028(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1029 {
  final int id;
  const Model1029(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1030 {
  final int id;
  const Model1030(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1031 {
  final int id;
  const Model1031(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1032 {
  final int id;
  const Model1032(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1033 {
  final int id;
  const Model1033(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1034 {
  final int id;
  const Model1034(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1035 {
  final int id;
  const Model1035(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1036 {
  final int id;
  const Model1036(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1037 {
  final int id;
  const Model1037(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1038 {
  final int id;
  const Model1038(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1039 {
  final int id;
  const Model1039(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1040 {
  final int id;
  const Model1040(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1041 {
  final int id;
  const Model1041(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1042 {
  final int id;
  const Model1042(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1043 {
  final int id;
  const Model1043(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1044 {
  final int id;
  const Model1044(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1045 {
  final int id;
  const Model1045(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1046 {
  final int id;
  const Model1046(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1047 {
  final int id;
  const Model1047(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1048 {
  final int id;
  const Model1048(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1049 {
  final int id;
  const Model1049(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1050 {
  final int id;
  const Model1050(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1051 {
  final int id;
  const Model1051(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1052 {
  final int id;
  const Model1052(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1053 {
  final int id;
  const Model1053(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1054 {
  final int id;
  const Model1054(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1055 {
  final int id;
  const Model1055(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1056 {
  final int id;
  const Model1056(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1057 {
  final int id;
  const Model1057(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1058 {
  final int id;
  const Model1058(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1059 {
  final int id;
  const Model1059(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1060 {
  final int id;
  const Model1060(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1061 {
  final int id;
  const Model1061(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1062 {
  final int id;
  const Model1062(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1063 {
  final int id;
  const Model1063(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1064 {
  final int id;
  const Model1064(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1065 {
  final int id;
  const Model1065(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1066 {
  final int id;
  const Model1066(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1067 {
  final int id;
  const Model1067(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1068 {
  final int id;
  const Model1068(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1069 {
  final int id;
  const Model1069(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1070 {
  final int id;
  const Model1070(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1071 {
  final int id;
  const Model1071(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1072 {
  final int id;
  const Model1072(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1073 {
  final int id;
  const Model1073(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1074 {
  final int id;
  const Model1074(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1075 {
  final int id;
  const Model1075(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1076 {
  final int id;
  const Model1076(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1077 {
  final int id;
  const Model1077(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1078 {
  final int id;
  const Model1078(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1079 {
  final int id;
  const Model1079(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1080 {
  final int id;
  const Model1080(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1081 {
  final int id;
  const Model1081(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1082 {
  final int id;
  const Model1082(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1083 {
  final int id;
  const Model1083(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1084 {
  final int id;
  const Model1084(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1085 {
  final int id;
  const Model1085(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1086 {
  final int id;
  const Model1086(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1087 {
  final int id;
  const Model1087(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1088 {
  final int id;
  const Model1088(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1089 {
  final int id;
  const Model1089(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1090 {
  final int id;
  const Model1090(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1091 {
  final int id;
  const Model1091(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1092 {
  final int id;
  const Model1092(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1093 {
  final int id;
  const Model1093(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1094 {
  final int id;
  const Model1094(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1095 {
  final int id;
  const Model1095(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1096 {
  final int id;
  const Model1096(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1097 {
  final int id;
  const Model1097(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1098 {
  final int id;
  const Model1098(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1099 {
  final int id;
  const Model1099(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1100 {
  final int id;
  const Model1100(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1101 {
  final int id;
  const Model1101(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1102 {
  final int id;
  const Model1102(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1103 {
  final int id;
  const Model1103(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1104 {
  final int id;
  const Model1104(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1105 {
  final int id;
  const Model1105(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1106 {
  final int id;
  const Model1106(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1107 {
  final int id;
  const Model1107(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1108 {
  final int id;
  const Model1108(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1109 {
  final int id;
  const Model1109(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1110 {
  final int id;
  const Model1110(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1111 {
  final int id;
  const Model1111(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1112 {
  final int id;
  const Model1112(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1113 {
  final int id;
  const Model1113(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1114 {
  final int id;
  const Model1114(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1115 {
  final int id;
  const Model1115(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1116 {
  final int id;
  const Model1116(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1117 {
  final int id;
  const Model1117(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1118 {
  final int id;
  const Model1118(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1119 {
  final int id;
  const Model1119(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1120 {
  final int id;
  const Model1120(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1121 {
  final int id;
  const Model1121(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1122 {
  final int id;
  const Model1122(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1123 {
  final int id;
  const Model1123(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1124 {
  final int id;
  const Model1124(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1125 {
  final int id;
  const Model1125(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1126 {
  final int id;
  const Model1126(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1127 {
  final int id;
  const Model1127(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1128 {
  final int id;
  const Model1128(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1129 {
  final int id;
  const Model1129(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1130 {
  final int id;
  const Model1130(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1131 {
  final int id;
  const Model1131(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1132 {
  final int id;
  const Model1132(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1133 {
  final int id;
  const Model1133(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1134 {
  final int id;
  const Model1134(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1135 {
  final int id;
  const Model1135(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1136 {
  final int id;
  const Model1136(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1137 {
  final int id;
  const Model1137(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1138 {
  final int id;
  const Model1138(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1139 {
  final int id;
  const Model1139(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1140 {
  final int id;
  const Model1140(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1141 {
  final int id;
  const Model1141(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1142 {
  final int id;
  const Model1142(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1143 {
  final int id;
  const Model1143(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1144 {
  final int id;
  const Model1144(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1145 {
  final int id;
  const Model1145(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1146 {
  final int id;
  const Model1146(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1147 {
  final int id;
  const Model1147(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1148 {
  final int id;
  const Model1148(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1149 {
  final int id;
  const Model1149(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1150 {
  final int id;
  const Model1150(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1151 {
  final int id;
  const Model1151(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1152 {
  final int id;
  const Model1152(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1153 {
  final int id;
  const Model1153(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1154 {
  final int id;
  const Model1154(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1155 {
  final int id;
  const Model1155(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1156 {
  final int id;
  const Model1156(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1157 {
  final int id;
  const Model1157(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1158 {
  final int id;
  const Model1158(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1159 {
  final int id;
  const Model1159(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1160 {
  final int id;
  const Model1160(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1161 {
  final int id;
  const Model1161(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1162 {
  final int id;
  const Model1162(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1163 {
  final int id;
  const Model1163(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1164 {
  final int id;
  const Model1164(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1165 {
  final int id;
  const Model1165(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1166 {
  final int id;
  const Model1166(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1167 {
  final int id;
  const Model1167(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1168 {
  final int id;
  const Model1168(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1169 {
  final int id;
  const Model1169(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1170 {
  final int id;
  const Model1170(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1171 {
  final int id;
  const Model1171(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1172 {
  final int id;
  const Model1172(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1173 {
  final int id;
  const Model1173(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1174 {
  final int id;
  const Model1174(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1175 {
  final int id;
  const Model1175(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1176 {
  final int id;
  const Model1176(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1177 {
  final int id;
  const Model1177(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1178 {
  final int id;
  const Model1178(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1179 {
  final int id;
  const Model1179(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1180 {
  final int id;
  const Model1180(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1181 {
  final int id;
  const Model1181(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1182 {
  final int id;
  const Model1182(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1183 {
  final int id;
  const Model1183(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1184 {
  final int id;
  const Model1184(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1185 {
  final int id;
  const Model1185(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1186 {
  final int id;
  const Model1186(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1187 {
  final int id;
  const Model1187(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1188 {
  final int id;
  const Model1188(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1189 {
  final int id;
  const Model1189(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1190 {
  final int id;
  const Model1190(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1191 {
  final int id;
  const Model1191(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1192 {
  final int id;
  const Model1192(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1193 {
  final int id;
  const Model1193(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1194 {
  final int id;
  const Model1194(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1195 {
  final int id;
  const Model1195(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1196 {
  final int id;
  const Model1196(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1197 {
  final int id;
  const Model1197(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1198 {
  final int id;
  const Model1198(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1199 {
  final int id;
  const Model1199(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1200 {
  final int id;
  const Model1200(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1201 {
  final int id;
  const Model1201(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1202 {
  final int id;
  const Model1202(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1203 {
  final int id;
  const Model1203(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1204 {
  final int id;
  const Model1204(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1205 {
  final int id;
  const Model1205(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1206 {
  final int id;
  const Model1206(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1207 {
  final int id;
  const Model1207(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1208 {
  final int id;
  const Model1208(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1209 {
  final int id;
  const Model1209(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1210 {
  final int id;
  const Model1210(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1211 {
  final int id;
  const Model1211(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1212 {
  final int id;
  const Model1212(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1213 {
  final int id;
  const Model1213(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1214 {
  final int id;
  const Model1214(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1215 {
  final int id;
  const Model1215(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1216 {
  final int id;
  const Model1216(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1217 {
  final int id;
  const Model1217(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1218 {
  final int id;
  const Model1218(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1219 {
  final int id;
  const Model1219(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1220 {
  final int id;
  const Model1220(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1221 {
  final int id;
  const Model1221(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1222 {
  final int id;
  const Model1222(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1223 {
  final int id;
  const Model1223(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1224 {
  final int id;
  const Model1224(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1225 {
  final int id;
  const Model1225(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1226 {
  final int id;
  const Model1226(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1227 {
  final int id;
  const Model1227(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1228 {
  final int id;
  const Model1228(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1229 {
  final int id;
  const Model1229(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1230 {
  final int id;
  const Model1230(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1231 {
  final int id;
  const Model1231(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1232 {
  final int id;
  const Model1232(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1233 {
  final int id;
  const Model1233(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1234 {
  final int id;
  const Model1234(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1235 {
  final int id;
  const Model1235(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1236 {
  final int id;
  const Model1236(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1237 {
  final int id;
  const Model1237(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1238 {
  final int id;
  const Model1238(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1239 {
  final int id;
  const Model1239(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1240 {
  final int id;
  const Model1240(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1241 {
  final int id;
  const Model1241(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1242 {
  final int id;
  const Model1242(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1243 {
  final int id;
  const Model1243(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1244 {
  final int id;
  const Model1244(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1245 {
  final int id;
  const Model1245(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1246 {
  final int id;
  const Model1246(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1247 {
  final int id;
  const Model1247(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1248 {
  final int id;
  const Model1248(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1249 {
  final int id;
  const Model1249(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1250 {
  final int id;
  const Model1250(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1251 {
  final int id;
  const Model1251(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1252 {
  final int id;
  const Model1252(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1253 {
  final int id;
  const Model1253(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1254 {
  final int id;
  const Model1254(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1255 {
  final int id;
  const Model1255(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1256 {
  final int id;
  const Model1256(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1257 {
  final int id;
  const Model1257(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1258 {
  final int id;
  const Model1258(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1259 {
  final int id;
  const Model1259(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1260 {
  final int id;
  const Model1260(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1261 {
  final int id;
  const Model1261(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1262 {
  final int id;
  const Model1262(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1263 {
  final int id;
  const Model1263(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1264 {
  final int id;
  const Model1264(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1265 {
  final int id;
  const Model1265(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1266 {
  final int id;
  const Model1266(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1267 {
  final int id;
  const Model1267(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1268 {
  final int id;
  const Model1268(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1269 {
  final int id;
  const Model1269(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1270 {
  final int id;
  const Model1270(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1271 {
  final int id;
  const Model1271(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1272 {
  final int id;
  const Model1272(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1273 {
  final int id;
  const Model1273(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1274 {
  final int id;
  const Model1274(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1275 {
  final int id;
  const Model1275(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1276 {
  final int id;
  const Model1276(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1277 {
  final int id;
  const Model1277(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1278 {
  final int id;
  const Model1278(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1279 {
  final int id;
  const Model1279(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1280 {
  final int id;
  const Model1280(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1281 {
  final int id;
  const Model1281(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1282 {
  final int id;
  const Model1282(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1283 {
  final int id;
  const Model1283(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1284 {
  final int id;
  const Model1284(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1285 {
  final int id;
  const Model1285(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1286 {
  final int id;
  const Model1286(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1287 {
  final int id;
  const Model1287(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1288 {
  final int id;
  const Model1288(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1289 {
  final int id;
  const Model1289(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1290 {
  final int id;
  const Model1290(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1291 {
  final int id;
  const Model1291(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1292 {
  final int id;
  const Model1292(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1293 {
  final int id;
  const Model1293(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1294 {
  final int id;
  const Model1294(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1295 {
  final int id;
  const Model1295(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1296 {
  final int id;
  const Model1296(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1297 {
  final int id;
  const Model1297(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1298 {
  final int id;
  const Model1298(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1299 {
  final int id;
  const Model1299(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1300 {
  final int id;
  const Model1300(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1301 {
  final int id;
  const Model1301(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1302 {
  final int id;
  const Model1302(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1303 {
  final int id;
  const Model1303(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1304 {
  final int id;
  const Model1304(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1305 {
  final int id;
  const Model1305(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1306 {
  final int id;
  const Model1306(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1307 {
  final int id;
  const Model1307(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1308 {
  final int id;
  const Model1308(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1309 {
  final int id;
  const Model1309(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1310 {
  final int id;
  const Model1310(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1311 {
  final int id;
  const Model1311(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1312 {
  final int id;
  const Model1312(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1313 {
  final int id;
  const Model1313(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1314 {
  final int id;
  const Model1314(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1315 {
  final int id;
  const Model1315(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1316 {
  final int id;
  const Model1316(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1317 {
  final int id;
  const Model1317(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1318 {
  final int id;
  const Model1318(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1319 {
  final int id;
  const Model1319(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1320 {
  final int id;
  const Model1320(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1321 {
  final int id;
  const Model1321(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1322 {
  final int id;
  const Model1322(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1323 {
  final int id;
  const Model1323(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1324 {
  final int id;
  const Model1324(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1325 {
  final int id;
  const Model1325(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1326 {
  final int id;
  const Model1326(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1327 {
  final int id;
  const Model1327(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1328 {
  final int id;
  const Model1328(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1329 {
  final int id;
  const Model1329(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1330 {
  final int id;
  const Model1330(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1331 {
  final int id;
  const Model1331(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1332 {
  final int id;
  const Model1332(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1333 {
  final int id;
  const Model1333(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1334 {
  final int id;
  const Model1334(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1335 {
  final int id;
  const Model1335(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1336 {
  final int id;
  const Model1336(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1337 {
  final int id;
  const Model1337(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1338 {
  final int id;
  const Model1338(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1339 {
  final int id;
  const Model1339(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1340 {
  final int id;
  const Model1340(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1341 {
  final int id;
  const Model1341(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1342 {
  final int id;
  const Model1342(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1343 {
  final int id;
  const Model1343(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1344 {
  final int id;
  const Model1344(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1345 {
  final int id;
  const Model1345(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1346 {
  final int id;
  const Model1346(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1347 {
  final int id;
  const Model1347(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1348 {
  final int id;
  const Model1348(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1349 {
  final int id;
  const Model1349(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1350 {
  final int id;
  const Model1350(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1351 {
  final int id;
  const Model1351(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1352 {
  final int id;
  const Model1352(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1353 {
  final int id;
  const Model1353(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1354 {
  final int id;
  const Model1354(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1355 {
  final int id;
  const Model1355(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1356 {
  final int id;
  const Model1356(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1357 {
  final int id;
  const Model1357(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1358 {
  final int id;
  const Model1358(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1359 {
  final int id;
  const Model1359(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1360 {
  final int id;
  const Model1360(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1361 {
  final int id;
  const Model1361(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1362 {
  final int id;
  const Model1362(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1363 {
  final int id;
  const Model1363(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1364 {
  final int id;
  const Model1364(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1365 {
  final int id;
  const Model1365(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1366 {
  final int id;
  const Model1366(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1367 {
  final int id;
  const Model1367(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1368 {
  final int id;
  const Model1368(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1369 {
  final int id;
  const Model1369(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1370 {
  final int id;
  const Model1370(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1371 {
  final int id;
  const Model1371(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1372 {
  final int id;
  const Model1372(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1373 {
  final int id;
  const Model1373(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1374 {
  final int id;
  const Model1374(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1375 {
  final int id;
  const Model1375(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1376 {
  final int id;
  const Model1376(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1377 {
  final int id;
  const Model1377(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1378 {
  final int id;
  const Model1378(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1379 {
  final int id;
  const Model1379(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1380 {
  final int id;
  const Model1380(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1381 {
  final int id;
  const Model1381(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1382 {
  final int id;
  const Model1382(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1383 {
  final int id;
  const Model1383(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1384 {
  final int id;
  const Model1384(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1385 {
  final int id;
  const Model1385(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1386 {
  final int id;
  const Model1386(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1387 {
  final int id;
  const Model1387(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1388 {
  final int id;
  const Model1388(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1389 {
  final int id;
  const Model1389(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1390 {
  final int id;
  const Model1390(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1391 {
  final int id;
  const Model1391(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1392 {
  final int id;
  const Model1392(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1393 {
  final int id;
  const Model1393(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1394 {
  final int id;
  const Model1394(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1395 {
  final int id;
  const Model1395(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1396 {
  final int id;
  const Model1396(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1397 {
  final int id;
  const Model1397(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1398 {
  final int id;
  const Model1398(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1399 {
  final int id;
  const Model1399(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1400 {
  final int id;
  const Model1400(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1401 {
  final int id;
  const Model1401(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1402 {
  final int id;
  const Model1402(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1403 {
  final int id;
  const Model1403(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1404 {
  final int id;
  const Model1404(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1405 {
  final int id;
  const Model1405(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1406 {
  final int id;
  const Model1406(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1407 {
  final int id;
  const Model1407(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1408 {
  final int id;
  const Model1408(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1409 {
  final int id;
  const Model1409(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1410 {
  final int id;
  const Model1410(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1411 {
  final int id;
  const Model1411(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1412 {
  final int id;
  const Model1412(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1413 {
  final int id;
  const Model1413(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1414 {
  final int id;
  const Model1414(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1415 {
  final int id;
  const Model1415(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1416 {
  final int id;
  const Model1416(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1417 {
  final int id;
  const Model1417(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1418 {
  final int id;
  const Model1418(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1419 {
  final int id;
  const Model1419(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1420 {
  final int id;
  const Model1420(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1421 {
  final int id;
  const Model1421(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1422 {
  final int id;
  const Model1422(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1423 {
  final int id;
  const Model1423(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1424 {
  final int id;
  const Model1424(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1425 {
  final int id;
  const Model1425(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1426 {
  final int id;
  const Model1426(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1427 {
  final int id;
  const Model1427(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1428 {
  final int id;
  const Model1428(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1429 {
  final int id;
  const Model1429(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1430 {
  final int id;
  const Model1430(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1431 {
  final int id;
  const Model1431(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1432 {
  final int id;
  const Model1432(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1433 {
  final int id;
  const Model1433(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1434 {
  final int id;
  const Model1434(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1435 {
  final int id;
  const Model1435(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1436 {
  final int id;
  const Model1436(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1437 {
  final int id;
  const Model1437(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1438 {
  final int id;
  const Model1438(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1439 {
  final int id;
  const Model1439(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1440 {
  final int id;
  const Model1440(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1441 {
  final int id;
  const Model1441(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1442 {
  final int id;
  const Model1442(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1443 {
  final int id;
  const Model1443(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1444 {
  final int id;
  const Model1444(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1445 {
  final int id;
  const Model1445(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1446 {
  final int id;
  const Model1446(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1447 {
  final int id;
  const Model1447(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1448 {
  final int id;
  const Model1448(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1449 {
  final int id;
  const Model1449(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1450 {
  final int id;
  const Model1450(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1451 {
  final int id;
  const Model1451(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1452 {
  final int id;
  const Model1452(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1453 {
  final int id;
  const Model1453(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1454 {
  final int id;
  const Model1454(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1455 {
  final int id;
  const Model1455(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1456 {
  final int id;
  const Model1456(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1457 {
  final int id;
  const Model1457(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1458 {
  final int id;
  const Model1458(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1459 {
  final int id;
  const Model1459(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1460 {
  final int id;
  const Model1460(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1461 {
  final int id;
  const Model1461(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1462 {
  final int id;
  const Model1462(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1463 {
  final int id;
  const Model1463(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1464 {
  final int id;
  const Model1464(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1465 {
  final int id;
  const Model1465(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1466 {
  final int id;
  const Model1466(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1467 {
  final int id;
  const Model1467(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1468 {
  final int id;
  const Model1468(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1469 {
  final int id;
  const Model1469(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1470 {
  final int id;
  const Model1470(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1471 {
  final int id;
  const Model1471(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1472 {
  final int id;
  const Model1472(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1473 {
  final int id;
  const Model1473(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1474 {
  final int id;
  const Model1474(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1475 {
  final int id;
  const Model1475(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1476 {
  final int id;
  const Model1476(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1477 {
  final int id;
  const Model1477(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1478 {
  final int id;
  const Model1478(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1479 {
  final int id;
  const Model1479(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1480 {
  final int id;
  const Model1480(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1481 {
  final int id;
  const Model1481(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1482 {
  final int id;
  const Model1482(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1483 {
  final int id;
  const Model1483(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1484 {
  final int id;
  const Model1484(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1485 {
  final int id;
  const Model1485(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1486 {
  final int id;
  const Model1486(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1487 {
  final int id;
  const Model1487(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1488 {
  final int id;
  const Model1488(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1489 {
  final int id;
  const Model1489(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1490 {
  final int id;
  const Model1490(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1491 {
  final int id;
  const Model1491(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1492 {
  final int id;
  const Model1492(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1493 {
  final int id;
  const Model1493(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1494 {
  final int id;
  const Model1494(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1495 {
  final int id;
  const Model1495(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1496 {
  final int id;
  const Model1496(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1497 {
  final int id;
  const Model1497(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1498 {
  final int id;
  const Model1498(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1499 {
  final int id;
  const Model1499(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1500 {
  final int id;
  const Model1500(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1501 {
  final int id;
  const Model1501(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1502 {
  final int id;
  const Model1502(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1503 {
  final int id;
  const Model1503(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1504 {
  final int id;
  const Model1504(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1505 {
  final int id;
  const Model1505(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1506 {
  final int id;
  const Model1506(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1507 {
  final int id;
  const Model1507(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1508 {
  final int id;
  const Model1508(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1509 {
  final int id;
  const Model1509(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1510 {
  final int id;
  const Model1510(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1511 {
  final int id;
  const Model1511(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1512 {
  final int id;
  const Model1512(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1513 {
  final int id;
  const Model1513(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1514 {
  final int id;
  const Model1514(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1515 {
  final int id;
  const Model1515(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1516 {
  final int id;
  const Model1516(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1517 {
  final int id;
  const Model1517(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1518 {
  final int id;
  const Model1518(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1519 {
  final int id;
  const Model1519(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1520 {
  final int id;
  const Model1520(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1521 {
  final int id;
  const Model1521(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1522 {
  final int id;
  const Model1522(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1523 {
  final int id;
  const Model1523(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1524 {
  final int id;
  const Model1524(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1525 {
  final int id;
  const Model1525(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1526 {
  final int id;
  const Model1526(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1527 {
  final int id;
  const Model1527(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1528 {
  final int id;
  const Model1528(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1529 {
  final int id;
  const Model1529(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1530 {
  final int id;
  const Model1530(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1531 {
  final int id;
  const Model1531(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1532 {
  final int id;
  const Model1532(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1533 {
  final int id;
  const Model1533(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1534 {
  final int id;
  const Model1534(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1535 {
  final int id;
  const Model1535(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1536 {
  final int id;
  const Model1536(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1537 {
  final int id;
  const Model1537(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1538 {
  final int id;
  const Model1538(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1539 {
  final int id;
  const Model1539(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1540 {
  final int id;
  const Model1540(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1541 {
  final int id;
  const Model1541(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1542 {
  final int id;
  const Model1542(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1543 {
  final int id;
  const Model1543(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1544 {
  final int id;
  const Model1544(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1545 {
  final int id;
  const Model1545(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1546 {
  final int id;
  const Model1546(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1547 {
  final int id;
  const Model1547(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1548 {
  final int id;
  const Model1548(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1549 {
  final int id;
  const Model1549(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1550 {
  final int id;
  const Model1550(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1551 {
  final int id;
  const Model1551(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1552 {
  final int id;
  const Model1552(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1553 {
  final int id;
  const Model1553(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1554 {
  final int id;
  const Model1554(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1555 {
  final int id;
  const Model1555(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1556 {
  final int id;
  const Model1556(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1557 {
  final int id;
  const Model1557(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1558 {
  final int id;
  const Model1558(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1559 {
  final int id;
  const Model1559(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1560 {
  final int id;
  const Model1560(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1561 {
  final int id;
  const Model1561(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1562 {
  final int id;
  const Model1562(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1563 {
  final int id;
  const Model1563(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1564 {
  final int id;
  const Model1564(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1565 {
  final int id;
  const Model1565(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1566 {
  final int id;
  const Model1566(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1567 {
  final int id;
  const Model1567(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1568 {
  final int id;
  const Model1568(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1569 {
  final int id;
  const Model1569(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1570 {
  final int id;
  const Model1570(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1571 {
  final int id;
  const Model1571(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1572 {
  final int id;
  const Model1572(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1573 {
  final int id;
  const Model1573(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1574 {
  final int id;
  const Model1574(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1575 {
  final int id;
  const Model1575(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1576 {
  final int id;
  const Model1576(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1577 {
  final int id;
  const Model1577(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1578 {
  final int id;
  const Model1578(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1579 {
  final int id;
  const Model1579(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1580 {
  final int id;
  const Model1580(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1581 {
  final int id;
  const Model1581(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1582 {
  final int id;
  const Model1582(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1583 {
  final int id;
  const Model1583(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1584 {
  final int id;
  const Model1584(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1585 {
  final int id;
  const Model1585(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1586 {
  final int id;
  const Model1586(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1587 {
  final int id;
  const Model1587(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1588 {
  final int id;
  const Model1588(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1589 {
  final int id;
  const Model1589(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1590 {
  final int id;
  const Model1590(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1591 {
  final int id;
  const Model1591(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1592 {
  final int id;
  const Model1592(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1593 {
  final int id;
  const Model1593(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1594 {
  final int id;
  const Model1594(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1595 {
  final int id;
  const Model1595(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1596 {
  final int id;
  const Model1596(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1597 {
  final int id;
  const Model1597(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1598 {
  final int id;
  const Model1598(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1599 {
  final int id;
  const Model1599(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1600 {
  final int id;
  const Model1600(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1601 {
  final int id;
  const Model1601(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1602 {
  final int id;
  const Model1602(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1603 {
  final int id;
  const Model1603(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1604 {
  final int id;
  const Model1604(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1605 {
  final int id;
  const Model1605(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1606 {
  final int id;
  const Model1606(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1607 {
  final int id;
  const Model1607(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1608 {
  final int id;
  const Model1608(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1609 {
  final int id;
  const Model1609(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1610 {
  final int id;
  const Model1610(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1611 {
  final int id;
  const Model1611(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1612 {
  final int id;
  const Model1612(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1613 {
  final int id;
  const Model1613(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1614 {
  final int id;
  const Model1614(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1615 {
  final int id;
  const Model1615(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1616 {
  final int id;
  const Model1616(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1617 {
  final int id;
  const Model1617(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1618 {
  final int id;
  const Model1618(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1619 {
  final int id;
  const Model1619(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1620 {
  final int id;
  const Model1620(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1621 {
  final int id;
  const Model1621(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1622 {
  final int id;
  const Model1622(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1623 {
  final int id;
  const Model1623(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1624 {
  final int id;
  const Model1624(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1625 {
  final int id;
  const Model1625(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1626 {
  final int id;
  const Model1626(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1627 {
  final int id;
  const Model1627(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1628 {
  final int id;
  const Model1628(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1629 {
  final int id;
  const Model1629(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1630 {
  final int id;
  const Model1630(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1631 {
  final int id;
  const Model1631(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1632 {
  final int id;
  const Model1632(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1633 {
  final int id;
  const Model1633(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1634 {
  final int id;
  const Model1634(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1635 {
  final int id;
  const Model1635(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1636 {
  final int id;
  const Model1636(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1637 {
  final int id;
  const Model1637(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1638 {
  final int id;
  const Model1638(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1639 {
  final int id;
  const Model1639(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1640 {
  final int id;
  const Model1640(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1641 {
  final int id;
  const Model1641(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1642 {
  final int id;
  const Model1642(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1643 {
  final int id;
  const Model1643(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1644 {
  final int id;
  const Model1644(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1645 {
  final int id;
  const Model1645(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1646 {
  final int id;
  const Model1646(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1647 {
  final int id;
  const Model1647(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1648 {
  final int id;
  const Model1648(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1649 {
  final int id;
  const Model1649(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1650 {
  final int id;
  const Model1650(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1651 {
  final int id;
  const Model1651(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1652 {
  final int id;
  const Model1652(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1653 {
  final int id;
  const Model1653(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1654 {
  final int id;
  const Model1654(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1655 {
  final int id;
  const Model1655(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1656 {
  final int id;
  const Model1656(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1657 {
  final int id;
  const Model1657(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1658 {
  final int id;
  const Model1658(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1659 {
  final int id;
  const Model1659(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1660 {
  final int id;
  const Model1660(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1661 {
  final int id;
  const Model1661(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1662 {
  final int id;
  const Model1662(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1663 {
  final int id;
  const Model1663(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1664 {
  final int id;
  const Model1664(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1665 {
  final int id;
  const Model1665(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1666 {
  final int id;
  const Model1666(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1667 {
  final int id;
  const Model1667(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1668 {
  final int id;
  const Model1668(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1669 {
  final int id;
  const Model1669(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1670 {
  final int id;
  const Model1670(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1671 {
  final int id;
  const Model1671(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1672 {
  final int id;
  const Model1672(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1673 {
  final int id;
  const Model1673(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1674 {
  final int id;
  const Model1674(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1675 {
  final int id;
  const Model1675(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1676 {
  final int id;
  const Model1676(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1677 {
  final int id;
  const Model1677(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1678 {
  final int id;
  const Model1678(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1679 {
  final int id;
  const Model1679(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1680 {
  final int id;
  const Model1680(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1681 {
  final int id;
  const Model1681(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1682 {
  final int id;
  const Model1682(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1683 {
  final int id;
  const Model1683(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1684 {
  final int id;
  const Model1684(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1685 {
  final int id;
  const Model1685(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1686 {
  final int id;
  const Model1686(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1687 {
  final int id;
  const Model1687(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1688 {
  final int id;
  const Model1688(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1689 {
  final int id;
  const Model1689(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1690 {
  final int id;
  const Model1690(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1691 {
  final int id;
  const Model1691(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1692 {
  final int id;
  const Model1692(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1693 {
  final int id;
  const Model1693(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1694 {
  final int id;
  const Model1694(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1695 {
  final int id;
  const Model1695(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1696 {
  final int id;
  const Model1696(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1697 {
  final int id;
  const Model1697(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1698 {
  final int id;
  const Model1698(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1699 {
  final int id;
  const Model1699(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1700 {
  final int id;
  const Model1700(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1701 {
  final int id;
  const Model1701(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1702 {
  final int id;
  const Model1702(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1703 {
  final int id;
  const Model1703(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1704 {
  final int id;
  const Model1704(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1705 {
  final int id;
  const Model1705(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1706 {
  final int id;
  const Model1706(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1707 {
  final int id;
  const Model1707(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1708 {
  final int id;
  const Model1708(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1709 {
  final int id;
  const Model1709(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1710 {
  final int id;
  const Model1710(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1711 {
  final int id;
  const Model1711(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1712 {
  final int id;
  const Model1712(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1713 {
  final int id;
  const Model1713(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1714 {
  final int id;
  const Model1714(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1715 {
  final int id;
  const Model1715(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1716 {
  final int id;
  const Model1716(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1717 {
  final int id;
  const Model1717(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1718 {
  final int id;
  const Model1718(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1719 {
  final int id;
  const Model1719(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1720 {
  final int id;
  const Model1720(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1721 {
  final int id;
  const Model1721(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1722 {
  final int id;
  const Model1722(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1723 {
  final int id;
  const Model1723(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1724 {
  final int id;
  const Model1724(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1725 {
  final int id;
  const Model1725(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1726 {
  final int id;
  const Model1726(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1727 {
  final int id;
  const Model1727(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1728 {
  final int id;
  const Model1728(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1729 {
  final int id;
  const Model1729(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1730 {
  final int id;
  const Model1730(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1731 {
  final int id;
  const Model1731(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1732 {
  final int id;
  const Model1732(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1733 {
  final int id;
  const Model1733(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1734 {
  final int id;
  const Model1734(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1735 {
  final int id;
  const Model1735(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1736 {
  final int id;
  const Model1736(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1737 {
  final int id;
  const Model1737(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1738 {
  final int id;
  const Model1738(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1739 {
  final int id;
  const Model1739(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1740 {
  final int id;
  const Model1740(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1741 {
  final int id;
  const Model1741(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1742 {
  final int id;
  const Model1742(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1743 {
  final int id;
  const Model1743(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1744 {
  final int id;
  const Model1744(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1745 {
  final int id;
  const Model1745(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1746 {
  final int id;
  const Model1746(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1747 {
  final int id;
  const Model1747(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1748 {
  final int id;
  const Model1748(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1749 {
  final int id;
  const Model1749(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1750 {
  final int id;
  const Model1750(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1751 {
  final int id;
  const Model1751(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1752 {
  final int id;
  const Model1752(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1753 {
  final int id;
  const Model1753(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1754 {
  final int id;
  const Model1754(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1755 {
  final int id;
  const Model1755(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1756 {
  final int id;
  const Model1756(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1757 {
  final int id;
  const Model1757(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1758 {
  final int id;
  const Model1758(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1759 {
  final int id;
  const Model1759(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1760 {
  final int id;
  const Model1760(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1761 {
  final int id;
  const Model1761(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1762 {
  final int id;
  const Model1762(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1763 {
  final int id;
  const Model1763(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1764 {
  final int id;
  const Model1764(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1765 {
  final int id;
  const Model1765(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1766 {
  final int id;
  const Model1766(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1767 {
  final int id;
  const Model1767(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1768 {
  final int id;
  const Model1768(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1769 {
  final int id;
  const Model1769(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1770 {
  final int id;
  const Model1770(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1771 {
  final int id;
  const Model1771(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1772 {
  final int id;
  const Model1772(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1773 {
  final int id;
  const Model1773(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1774 {
  final int id;
  const Model1774(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1775 {
  final int id;
  const Model1775(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1776 {
  final int id;
  const Model1776(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1777 {
  final int id;
  const Model1777(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1778 {
  final int id;
  const Model1778(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1779 {
  final int id;
  const Model1779(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1780 {
  final int id;
  const Model1780(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1781 {
  final int id;
  const Model1781(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1782 {
  final int id;
  const Model1782(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1783 {
  final int id;
  const Model1783(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1784 {
  final int id;
  const Model1784(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1785 {
  final int id;
  const Model1785(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1786 {
  final int id;
  const Model1786(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1787 {
  final int id;
  const Model1787(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1788 {
  final int id;
  const Model1788(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1789 {
  final int id;
  const Model1789(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1790 {
  final int id;
  const Model1790(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1791 {
  final int id;
  const Model1791(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1792 {
  final int id;
  const Model1792(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1793 {
  final int id;
  const Model1793(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1794 {
  final int id;
  const Model1794(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1795 {
  final int id;
  const Model1795(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1796 {
  final int id;
  const Model1796(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1797 {
  final int id;
  const Model1797(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1798 {
  final int id;
  const Model1798(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1799 {
  final int id;
  const Model1799(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1800 {
  final int id;
  const Model1800(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1801 {
  final int id;
  const Model1801(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1802 {
  final int id;
  const Model1802(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1803 {
  final int id;
  const Model1803(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1804 {
  final int id;
  const Model1804(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1805 {
  final int id;
  const Model1805(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1806 {
  final int id;
  const Model1806(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1807 {
  final int id;
  const Model1807(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1808 {
  final int id;
  const Model1808(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1809 {
  final int id;
  const Model1809(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1810 {
  final int id;
  const Model1810(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1811 {
  final int id;
  const Model1811(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1812 {
  final int id;
  const Model1812(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1813 {
  final int id;
  const Model1813(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1814 {
  final int id;
  const Model1814(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1815 {
  final int id;
  const Model1815(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1816 {
  final int id;
  const Model1816(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1817 {
  final int id;
  const Model1817(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1818 {
  final int id;
  const Model1818(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1819 {
  final int id;
  const Model1819(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1820 {
  final int id;
  const Model1820(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1821 {
  final int id;
  const Model1821(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1822 {
  final int id;
  const Model1822(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1823 {
  final int id;
  const Model1823(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1824 {
  final int id;
  const Model1824(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1825 {
  final int id;
  const Model1825(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1826 {
  final int id;
  const Model1826(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1827 {
  final int id;
  const Model1827(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1828 {
  final int id;
  const Model1828(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1829 {
  final int id;
  const Model1829(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1830 {
  final int id;
  const Model1830(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1831 {
  final int id;
  const Model1831(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1832 {
  final int id;
  const Model1832(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1833 {
  final int id;
  const Model1833(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1834 {
  final int id;
  const Model1834(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1835 {
  final int id;
  const Model1835(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1836 {
  final int id;
  const Model1836(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1837 {
  final int id;
  const Model1837(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1838 {
  final int id;
  const Model1838(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1839 {
  final int id;
  const Model1839(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1840 {
  final int id;
  const Model1840(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1841 {
  final int id;
  const Model1841(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1842 {
  final int id;
  const Model1842(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1843 {
  final int id;
  const Model1843(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1844 {
  final int id;
  const Model1844(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1845 {
  final int id;
  const Model1845(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1846 {
  final int id;
  const Model1846(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1847 {
  final int id;
  const Model1847(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1848 {
  final int id;
  const Model1848(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1849 {
  final int id;
  const Model1849(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1850 {
  final int id;
  const Model1850(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1851 {
  final int id;
  const Model1851(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1852 {
  final int id;
  const Model1852(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1853 {
  final int id;
  const Model1853(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1854 {
  final int id;
  const Model1854(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1855 {
  final int id;
  const Model1855(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1856 {
  final int id;
  const Model1856(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1857 {
  final int id;
  const Model1857(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1858 {
  final int id;
  const Model1858(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1859 {
  final int id;
  const Model1859(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1860 {
  final int id;
  const Model1860(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1861 {
  final int id;
  const Model1861(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1862 {
  final int id;
  const Model1862(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1863 {
  final int id;
  const Model1863(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1864 {
  final int id;
  const Model1864(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1865 {
  final int id;
  const Model1865(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1866 {
  final int id;
  const Model1866(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1867 {
  final int id;
  const Model1867(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1868 {
  final int id;
  const Model1868(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1869 {
  final int id;
  const Model1869(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1870 {
  final int id;
  const Model1870(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1871 {
  final int id;
  const Model1871(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1872 {
  final int id;
  const Model1872(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1873 {
  final int id;
  const Model1873(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1874 {
  final int id;
  const Model1874(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1875 {
  final int id;
  const Model1875(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1876 {
  final int id;
  const Model1876(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1877 {
  final int id;
  const Model1877(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1878 {
  final int id;
  const Model1878(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1879 {
  final int id;
  const Model1879(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1880 {
  final int id;
  const Model1880(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1881 {
  final int id;
  const Model1881(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1882 {
  final int id;
  const Model1882(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1883 {
  final int id;
  const Model1883(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1884 {
  final int id;
  const Model1884(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1885 {
  final int id;
  const Model1885(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1886 {
  final int id;
  const Model1886(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1887 {
  final int id;
  const Model1887(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1888 {
  final int id;
  const Model1888(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1889 {
  final int id;
  const Model1889(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1890 {
  final int id;
  const Model1890(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1891 {
  final int id;
  const Model1891(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1892 {
  final int id;
  const Model1892(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1893 {
  final int id;
  const Model1893(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1894 {
  final int id;
  const Model1894(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1895 {
  final int id;
  const Model1895(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1896 {
  final int id;
  const Model1896(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1897 {
  final int id;
  const Model1897(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1898 {
  final int id;
  const Model1898(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1899 {
  final int id;
  const Model1899(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1900 {
  final int id;
  const Model1900(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1901 {
  final int id;
  const Model1901(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1902 {
  final int id;
  const Model1902(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1903 {
  final int id;
  const Model1903(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1904 {
  final int id;
  const Model1904(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1905 {
  final int id;
  const Model1905(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1906 {
  final int id;
  const Model1906(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1907 {
  final int id;
  const Model1907(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1908 {
  final int id;
  const Model1908(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1909 {
  final int id;
  const Model1909(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1910 {
  final int id;
  const Model1910(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1911 {
  final int id;
  const Model1911(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1912 {
  final int id;
  const Model1912(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1913 {
  final int id;
  const Model1913(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1914 {
  final int id;
  const Model1914(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1915 {
  final int id;
  const Model1915(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1916 {
  final int id;
  const Model1916(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1917 {
  final int id;
  const Model1917(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1918 {
  final int id;
  const Model1918(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1919 {
  final int id;
  const Model1919(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1920 {
  final int id;
  const Model1920(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1921 {
  final int id;
  const Model1921(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1922 {
  final int id;
  const Model1922(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1923 {
  final int id;
  const Model1923(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1924 {
  final int id;
  const Model1924(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1925 {
  final int id;
  const Model1925(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1926 {
  final int id;
  const Model1926(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1927 {
  final int id;
  const Model1927(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1928 {
  final int id;
  const Model1928(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1929 {
  final int id;
  const Model1929(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1930 {
  final int id;
  const Model1930(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1931 {
  final int id;
  const Model1931(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1932 {
  final int id;
  const Model1932(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1933 {
  final int id;
  const Model1933(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1934 {
  final int id;
  const Model1934(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1935 {
  final int id;
  const Model1935(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1936 {
  final int id;
  const Model1936(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1937 {
  final int id;
  const Model1937(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1938 {
  final int id;
  const Model1938(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1939 {
  final int id;
  const Model1939(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1940 {
  final int id;
  const Model1940(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1941 {
  final int id;
  const Model1941(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1942 {
  final int id;
  const Model1942(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1943 {
  final int id;
  const Model1943(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1944 {
  final int id;
  const Model1944(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1945 {
  final int id;
  const Model1945(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1946 {
  final int id;
  const Model1946(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1947 {
  final int id;
  const Model1947(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1948 {
  final int id;
  const Model1948(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1949 {
  final int id;
  const Model1949(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1950 {
  final int id;
  const Model1950(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1951 {
  final int id;
  const Model1951(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1952 {
  final int id;
  const Model1952(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1953 {
  final int id;
  const Model1953(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1954 {
  final int id;
  const Model1954(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1955 {
  final int id;
  const Model1955(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1956 {
  final int id;
  const Model1956(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1957 {
  final int id;
  const Model1957(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1958 {
  final int id;
  const Model1958(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1959 {
  final int id;
  const Model1959(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1960 {
  final int id;
  const Model1960(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1961 {
  final int id;
  const Model1961(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1962 {
  final int id;
  const Model1962(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1963 {
  final int id;
  const Model1963(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1964 {
  final int id;
  const Model1964(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1965 {
  final int id;
  const Model1965(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1966 {
  final int id;
  const Model1966(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1967 {
  final int id;
  const Model1967(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1968 {
  final int id;
  const Model1968(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1969 {
  final int id;
  const Model1969(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1970 {
  final int id;
  const Model1970(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1971 {
  final int id;
  const Model1971(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1972 {
  final int id;
  const Model1972(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1973 {
  final int id;
  const Model1973(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1974 {
  final int id;
  const Model1974(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1975 {
  final int id;
  const Model1975(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1976 {
  final int id;
  const Model1976(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1977 {
  final int id;
  const Model1977(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1978 {
  final int id;
  const Model1978(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1979 {
  final int id;
  const Model1979(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1980 {
  final int id;
  const Model1980(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1981 {
  final int id;
  const Model1981(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1982 {
  final int id;
  const Model1982(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model1983 {
  final int id;
  const Model1983(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model1984 {
  final int id;
  const Model1984(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1985 {
  final int id;
  const Model1985(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1986 {
  final int id;
  const Model1986(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1987 {
  final int id;
  const Model1987(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model1988 {
  final int id;
  const Model1988(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1989 {
  final int id;
  const Model1989(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1990 {
  final int id;
  const Model1990(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model1991 {
  final int id;
  const Model1991(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1992 {
  final int id;
  const Model1992(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1993 {
  final int id;
  const Model1993(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model1994 {
  final int id;
  const Model1994(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1995 {
  final int id;
  const Model1995(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model1996 {
  final int id;
  const Model1996(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model1997 {
  final int id;
  const Model1997(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model1998 {
  final int id;
  const Model1998(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model1999 {
  final int id;
  const Model1999(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2000 {
  final int id;
  const Model2000(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2001 {
  final int id;
  const Model2001(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2002 {
  final int id;
  const Model2002(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2003 {
  final int id;
  const Model2003(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2004 {
  final int id;
  const Model2004(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2005 {
  final int id;
  const Model2005(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2006 {
  final int id;
  const Model2006(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2007 {
  final int id;
  const Model2007(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2008 {
  final int id;
  const Model2008(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2009 {
  final int id;
  const Model2009(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2010 {
  final int id;
  const Model2010(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2011 {
  final int id;
  const Model2011(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2012 {
  final int id;
  const Model2012(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2013 {
  final int id;
  const Model2013(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2014 {
  final int id;
  const Model2014(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2015 {
  final int id;
  const Model2015(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2016 {
  final int id;
  const Model2016(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2017 {
  final int id;
  const Model2017(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2018 {
  final int id;
  const Model2018(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2019 {
  final int id;
  const Model2019(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2020 {
  final int id;
  const Model2020(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2021 {
  final int id;
  const Model2021(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2022 {
  final int id;
  const Model2022(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2023 {
  final int id;
  const Model2023(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2024 {
  final int id;
  const Model2024(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2025 {
  final int id;
  const Model2025(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2026 {
  final int id;
  const Model2026(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2027 {
  final int id;
  const Model2027(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2028 {
  final int id;
  const Model2028(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2029 {
  final int id;
  const Model2029(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2030 {
  final int id;
  const Model2030(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2031 {
  final int id;
  const Model2031(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2032 {
  final int id;
  const Model2032(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2033 {
  final int id;
  const Model2033(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2034 {
  final int id;
  const Model2034(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2035 {
  final int id;
  const Model2035(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2036 {
  final int id;
  const Model2036(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2037 {
  final int id;
  const Model2037(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2038 {
  final int id;
  const Model2038(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2039 {
  final int id;
  const Model2039(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2040 {
  final int id;
  const Model2040(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2041 {
  final int id;
  const Model2041(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2042 {
  final int id;
  const Model2042(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2043 {
  final int id;
  const Model2043(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2044 {
  final int id;
  const Model2044(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2045 {
  final int id;
  const Model2045(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2046 {
  final int id;
  const Model2046(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2047 {
  final int id;
  const Model2047(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2048 {
  final int id;
  const Model2048(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2049 {
  final int id;
  const Model2049(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2050 {
  final int id;
  const Model2050(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2051 {
  final int id;
  const Model2051(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2052 {
  final int id;
  const Model2052(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2053 {
  final int id;
  const Model2053(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2054 {
  final int id;
  const Model2054(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2055 {
  final int id;
  const Model2055(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2056 {
  final int id;
  const Model2056(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2057 {
  final int id;
  const Model2057(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2058 {
  final int id;
  const Model2058(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2059 {
  final int id;
  const Model2059(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2060 {
  final int id;
  const Model2060(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2061 {
  final int id;
  const Model2061(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2062 {
  final int id;
  const Model2062(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2063 {
  final int id;
  const Model2063(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2064 {
  final int id;
  const Model2064(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2065 {
  final int id;
  const Model2065(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2066 {
  final int id;
  const Model2066(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2067 {
  final int id;
  const Model2067(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2068 {
  final int id;
  const Model2068(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2069 {
  final int id;
  const Model2069(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2070 {
  final int id;
  const Model2070(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2071 {
  final int id;
  const Model2071(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2072 {
  final int id;
  const Model2072(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2073 {
  final int id;
  const Model2073(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2074 {
  final int id;
  const Model2074(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2075 {
  final int id;
  const Model2075(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2076 {
  final int id;
  const Model2076(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2077 {
  final int id;
  const Model2077(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2078 {
  final int id;
  const Model2078(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2079 {
  final int id;
  const Model2079(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2080 {
  final int id;
  const Model2080(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2081 {
  final int id;
  const Model2081(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2082 {
  final int id;
  const Model2082(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2083 {
  final int id;
  const Model2083(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2084 {
  final int id;
  const Model2084(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2085 {
  final int id;
  const Model2085(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2086 {
  final int id;
  const Model2086(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2087 {
  final int id;
  const Model2087(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2088 {
  final int id;
  const Model2088(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2089 {
  final int id;
  const Model2089(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2090 {
  final int id;
  const Model2090(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2091 {
  final int id;
  const Model2091(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2092 {
  final int id;
  const Model2092(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2093 {
  final int id;
  const Model2093(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2094 {
  final int id;
  const Model2094(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2095 {
  final int id;
  const Model2095(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2096 {
  final int id;
  const Model2096(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2097 {
  final int id;
  const Model2097(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2098 {
  final int id;
  const Model2098(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2099 {
  final int id;
  const Model2099(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2100 {
  final int id;
  const Model2100(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2101 {
  final int id;
  const Model2101(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2102 {
  final int id;
  const Model2102(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2103 {
  final int id;
  const Model2103(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2104 {
  final int id;
  const Model2104(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2105 {
  final int id;
  const Model2105(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2106 {
  final int id;
  const Model2106(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2107 {
  final int id;
  const Model2107(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2108 {
  final int id;
  const Model2108(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2109 {
  final int id;
  const Model2109(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2110 {
  final int id;
  const Model2110(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2111 {
  final int id;
  const Model2111(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2112 {
  final int id;
  const Model2112(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2113 {
  final int id;
  const Model2113(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2114 {
  final int id;
  const Model2114(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2115 {
  final int id;
  const Model2115(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2116 {
  final int id;
  const Model2116(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2117 {
  final int id;
  const Model2117(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2118 {
  final int id;
  const Model2118(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2119 {
  final int id;
  const Model2119(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2120 {
  final int id;
  const Model2120(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2121 {
  final int id;
  const Model2121(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2122 {
  final int id;
  const Model2122(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2123 {
  final int id;
  const Model2123(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2124 {
  final int id;
  const Model2124(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2125 {
  final int id;
  const Model2125(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2126 {
  final int id;
  const Model2126(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2127 {
  final int id;
  const Model2127(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2128 {
  final int id;
  const Model2128(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2129 {
  final int id;
  const Model2129(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2130 {
  final int id;
  const Model2130(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2131 {
  final int id;
  const Model2131(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2132 {
  final int id;
  const Model2132(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2133 {
  final int id;
  const Model2133(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2134 {
  final int id;
  const Model2134(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2135 {
  final int id;
  const Model2135(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2136 {
  final int id;
  const Model2136(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2137 {
  final int id;
  const Model2137(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2138 {
  final int id;
  const Model2138(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2139 {
  final int id;
  const Model2139(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2140 {
  final int id;
  const Model2140(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2141 {
  final int id;
  const Model2141(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2142 {
  final int id;
  const Model2142(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2143 {
  final int id;
  const Model2143(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2144 {
  final int id;
  const Model2144(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2145 {
  final int id;
  const Model2145(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2146 {
  final int id;
  const Model2146(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2147 {
  final int id;
  const Model2147(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2148 {
  final int id;
  const Model2148(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2149 {
  final int id;
  const Model2149(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2150 {
  final int id;
  const Model2150(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2151 {
  final int id;
  const Model2151(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2152 {
  final int id;
  const Model2152(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2153 {
  final int id;
  const Model2153(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2154 {
  final int id;
  const Model2154(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2155 {
  final int id;
  const Model2155(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2156 {
  final int id;
  const Model2156(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2157 {
  final int id;
  const Model2157(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2158 {
  final int id;
  const Model2158(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2159 {
  final int id;
  const Model2159(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2160 {
  final int id;
  const Model2160(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2161 {
  final int id;
  const Model2161(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2162 {
  final int id;
  const Model2162(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2163 {
  final int id;
  const Model2163(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2164 {
  final int id;
  const Model2164(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2165 {
  final int id;
  const Model2165(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2166 {
  final int id;
  const Model2166(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2167 {
  final int id;
  const Model2167(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2168 {
  final int id;
  const Model2168(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2169 {
  final int id;
  const Model2169(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2170 {
  final int id;
  const Model2170(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2171 {
  final int id;
  const Model2171(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2172 {
  final int id;
  const Model2172(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2173 {
  final int id;
  const Model2173(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2174 {
  final int id;
  const Model2174(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2175 {
  final int id;
  const Model2175(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2176 {
  final int id;
  const Model2176(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2177 {
  final int id;
  const Model2177(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2178 {
  final int id;
  const Model2178(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2179 {
  final int id;
  const Model2179(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2180 {
  final int id;
  const Model2180(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2181 {
  final int id;
  const Model2181(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2182 {
  final int id;
  const Model2182(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2183 {
  final int id;
  const Model2183(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2184 {
  final int id;
  const Model2184(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2185 {
  final int id;
  const Model2185(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2186 {
  final int id;
  const Model2186(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2187 {
  final int id;
  const Model2187(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2188 {
  final int id;
  const Model2188(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2189 {
  final int id;
  const Model2189(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2190 {
  final int id;
  const Model2190(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2191 {
  final int id;
  const Model2191(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2192 {
  final int id;
  const Model2192(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2193 {
  final int id;
  const Model2193(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2194 {
  final int id;
  const Model2194(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2195 {
  final int id;
  const Model2195(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2196 {
  final int id;
  const Model2196(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2197 {
  final int id;
  const Model2197(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2198 {
  final int id;
  const Model2198(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2199 {
  final int id;
  const Model2199(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2200 {
  final int id;
  const Model2200(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2201 {
  final int id;
  const Model2201(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2202 {
  final int id;
  const Model2202(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2203 {
  final int id;
  const Model2203(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2204 {
  final int id;
  const Model2204(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2205 {
  final int id;
  const Model2205(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2206 {
  final int id;
  const Model2206(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2207 {
  final int id;
  const Model2207(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2208 {
  final int id;
  const Model2208(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2209 {
  final int id;
  const Model2209(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2210 {
  final int id;
  const Model2210(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2211 {
  final int id;
  const Model2211(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2212 {
  final int id;
  const Model2212(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2213 {
  final int id;
  const Model2213(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2214 {
  final int id;
  const Model2214(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2215 {
  final int id;
  const Model2215(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2216 {
  final int id;
  const Model2216(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2217 {
  final int id;
  const Model2217(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2218 {
  final int id;
  const Model2218(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2219 {
  final int id;
  const Model2219(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2220 {
  final int id;
  const Model2220(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2221 {
  final int id;
  const Model2221(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2222 {
  final int id;
  const Model2222(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2223 {
  final int id;
  const Model2223(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2224 {
  final int id;
  const Model2224(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2225 {
  final int id;
  const Model2225(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2226 {
  final int id;
  const Model2226(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 62 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2227 {
  final int id;
  const Model2227(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2228 {
  final int id;
  const Model2228(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2229 {
  final int id;
  const Model2229(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2230 {
  final int id;
  const Model2230(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2231 {
  final int id;
  const Model2231(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2232 {
  final int id;
  const Model2232(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2233 {
  final int id;
  const Model2233(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2234 {
  final int id;
  const Model2234(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2235 {
  final int id;
  const Model2235(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2236 {
  final int id;
  const Model2236(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2237 {
  final int id;
  const Model2237(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2238 {
  final int id;
  const Model2238(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2239 {
  final int id;
  const Model2239(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2240 {
  final int id;
  const Model2240(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2241 {
  final int id;
  const Model2241(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2242 {
  final int id;
  const Model2242(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2243 {
  final int id;
  const Model2243(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2244 {
  final int id;
  const Model2244(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2245 {
  final int id;
  const Model2245(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2246 {
  final int id;
  const Model2246(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2247 {
  final int id;
  const Model2247(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2248 {
  final int id;
  const Model2248(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2249 {
  final int id;
  const Model2249(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2250 {
  final int id;
  const Model2250(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2251 {
  final int id;
  const Model2251(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2252 {
  final int id;
  const Model2252(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2253 {
  final int id;
  const Model2253(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2254 {
  final int id;
  const Model2254(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2255 {
  final int id;
  const Model2255(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2256 {
  final int id;
  const Model2256(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2257 {
  final int id;
  const Model2257(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2258 {
  final int id;
  const Model2258(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2259 {
  final int id;
  const Model2259(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2260 {
  final int id;
  const Model2260(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2261 {
  final int id;
  const Model2261(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2262 {
  final int id;
  const Model2262(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2263 {
  final int id;
  const Model2263(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2264 {
  final int id;
  const Model2264(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2265 {
  final int id;
  const Model2265(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2266 {
  final int id;
  const Model2266(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2267 {
  final int id;
  const Model2267(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2268 {
  final int id;
  const Model2268(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2269 {
  final int id;
  const Model2269(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2270 {
  final int id;
  const Model2270(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2271 {
  final int id;
  const Model2271(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2272 {
  final int id;
  const Model2272(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2273 {
  final int id;
  const Model2273(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2274 {
  final int id;
  const Model2274(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2275 {
  final int id;
  const Model2275(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2276 {
  final int id;
  const Model2276(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2277 {
  final int id;
  const Model2277(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2278 {
  final int id;
  const Model2278(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2279 {
  final int id;
  const Model2279(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2280 {
  final int id;
  const Model2280(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2281 {
  final int id;
  const Model2281(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2282 {
  final int id;
  const Model2282(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2283 {
  final int id;
  const Model2283(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2284 {
  final int id;
  const Model2284(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2285 {
  final int id;
  const Model2285(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2286 {
  final int id;
  const Model2286(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2287 {
  final int id;
  const Model2287(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2288 {
  final int id;
  const Model2288(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2289 {
  final int id;
  const Model2289(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2290 {
  final int id;
  const Model2290(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2291 {
  final int id;
  const Model2291(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2292 {
  final int id;
  const Model2292(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2293 {
  final int id;
  const Model2293(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2294 {
  final int id;
  const Model2294(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2295 {
  final int id;
  const Model2295(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2296 {
  final int id;
  const Model2296(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2297 {
  final int id;
  const Model2297(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2298 {
  final int id;
  const Model2298(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2299 {
  final int id;
  const Model2299(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2300 {
  final int id;
  const Model2300(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2301 {
  final int id;
  const Model2301(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2302 {
  final int id;
  const Model2302(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2303 {
  final int id;
  const Model2303(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2304 {
  final int id;
  const Model2304(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2305 {
  final int id;
  const Model2305(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2306 {
  final int id;
  const Model2306(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2307 {
  final int id;
  const Model2307(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2308 {
  final int id;
  const Model2308(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2309 {
  final int id;
  const Model2309(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2310 {
  final int id;
  const Model2310(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2311 {
  final int id;
  const Model2311(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2312 {
  final int id;
  const Model2312(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2313 {
  final int id;
  const Model2313(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2314 {
  final int id;
  const Model2314(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2315 {
  final int id;
  const Model2315(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2316 {
  final int id;
  const Model2316(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2317 {
  final int id;
  const Model2317(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2318 {
  final int id;
  const Model2318(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2319 {
  final int id;
  const Model2319(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2320 {
  final int id;
  const Model2320(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2321 {
  final int id;
  const Model2321(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2322 {
  final int id;
  const Model2322(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2323 {
  final int id;
  const Model2323(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2324 {
  final int id;
  const Model2324(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2325 {
  final int id;
  const Model2325(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2326 {
  final int id;
  const Model2326(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2327 {
  final int id;
  const Model2327(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2328 {
  final int id;
  const Model2328(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2329 {
  final int id;
  const Model2329(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2330 {
  final int id;
  const Model2330(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2331 {
  final int id;
  const Model2331(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2332 {
  final int id;
  const Model2332(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2333 {
  final int id;
  const Model2333(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2334 {
  final int id;
  const Model2334(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2335 {
  final int id;
  const Model2335(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2336 {
  final int id;
  const Model2336(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2337 {
  final int id;
  const Model2337(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2338 {
  final int id;
  const Model2338(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2339 {
  final int id;
  const Model2339(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2340 {
  final int id;
  const Model2340(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2341 {
  final int id;
  const Model2341(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2342 {
  final int id;
  const Model2342(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2343 {
  final int id;
  const Model2343(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2344 {
  final int id;
  const Model2344(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2345 {
  final int id;
  const Model2345(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2346 {
  final int id;
  const Model2346(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2347 {
  final int id;
  const Model2347(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2348 {
  final int id;
  const Model2348(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2349 {
  final int id;
  const Model2349(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2350 {
  final int id;
  const Model2350(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2351 {
  final int id;
  const Model2351(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2352 {
  final int id;
  const Model2352(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 24 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2353 {
  final int id;
  const Model2353(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2354 {
  final int id;
  const Model2354(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2355 {
  final int id;
  const Model2355(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2356 {
  final int id;
  const Model2356(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2357 {
  final int id;
  const Model2357(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 19 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2358 {
  final int id;
  const Model2358(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2359 {
  final int id;
  const Model2359(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2360 {
  final int id;
  const Model2360(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2361 {
  final int id;
  const Model2361(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2362 {
  final int id;
  const Model2362(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2363 {
  final int id;
  const Model2363(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2364 {
  final int id;
  const Model2364(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2365 {
  final int id;
  const Model2365(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2366 {
  final int id;
  const Model2366(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2367 {
  final int id;
  const Model2367(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2368 {
  final int id;
  const Model2368(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2369 {
  final int id;
  const Model2369(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2370 {
  final int id;
  const Model2370(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2371 {
  final int id;
  const Model2371(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2372 {
  final int id;
  const Model2372(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2373 {
  final int id;
  const Model2373(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2374 {
  final int id;
  const Model2374(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2375 {
  final int id;
  const Model2375(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2376 {
  final int id;
  const Model2376(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2377 {
  final int id;
  const Model2377(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2378 {
  final int id;
  const Model2378(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2379 {
  final int id;
  const Model2379(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2380 {
  final int id;
  const Model2380(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2381 {
  final int id;
  const Model2381(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2382 {
  final int id;
  const Model2382(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2383 {
  final int id;
  const Model2383(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2384 {
  final int id;
  const Model2384(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2385 {
  final int id;
  const Model2385(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2386 {
  final int id;
  const Model2386(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2387 {
  final int id;
  const Model2387(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2388 {
  final int id;
  const Model2388(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2389 {
  final int id;
  const Model2389(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2390 {
  final int id;
  const Model2390(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2391 {
  final int id;
  const Model2391(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2392 {
  final int id;
  const Model2392(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2393 {
  final int id;
  const Model2393(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2394 {
  final int id;
  const Model2394(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2395 {
  final int id;
  const Model2395(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2396 {
  final int id;
  const Model2396(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2397 {
  final int id;
  const Model2397(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2398 {
  final int id;
  const Model2398(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2399 {
  final int id;
  const Model2399(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2400 {
  final int id;
  const Model2400(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2401 {
  final int id;
  const Model2401(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2402 {
  final int id;
  const Model2402(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2403 {
  final int id;
  const Model2403(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2404 {
  final int id;
  const Model2404(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2405 {
  final int id;
  const Model2405(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2406 {
  final int id;
  const Model2406(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2407 {
  final int id;
  const Model2407(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2408 {
  final int id;
  const Model2408(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2409 {
  final int id;
  const Model2409(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2410 {
  final int id;
  const Model2410(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2411 {
  final int id;
  const Model2411(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2412 {
  final int id;
  const Model2412(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2413 {
  final int id;
  const Model2413(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2414 {
  final int id;
  const Model2414(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2415 {
  final int id;
  const Model2415(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2416 {
  final int id;
  const Model2416(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2417 {
  final int id;
  const Model2417(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2418 {
  final int id;
  const Model2418(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2419 {
  final int id;
  const Model2419(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2420 {
  final int id;
  const Model2420(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2421 {
  final int id;
  const Model2421(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2422 {
  final int id;
  const Model2422(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2423 {
  final int id;
  const Model2423(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2424 {
  final int id;
  const Model2424(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2425 {
  final int id;
  const Model2425(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2426 {
  final int id;
  const Model2426(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2427 {
  final int id;
  const Model2427(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2428 {
  final int id;
  const Model2428(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2429 {
  final int id;
  const Model2429(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2430 {
  final int id;
  const Model2430(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2431 {
  final int id;
  const Model2431(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2432 {
  final int id;
  const Model2432(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2433 {
  final int id;
  const Model2433(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2434 {
  final int id;
  const Model2434(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2435 {
  final int id;
  const Model2435(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2436 {
  final int id;
  const Model2436(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2437 {
  final int id;
  const Model2437(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2438 {
  final int id;
  const Model2438(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2439 {
  final int id;
  const Model2439(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2440 {
  final int id;
  const Model2440(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2441 {
  final int id;
  const Model2441(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2442 {
  final int id;
  const Model2442(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2443 {
  final int id;
  const Model2443(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2444 {
  final int id;
  const Model2444(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2445 {
  final int id;
  const Model2445(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2446 {
  final int id;
  const Model2446(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2447 {
  final int id;
  const Model2447(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2448 {
  final int id;
  const Model2448(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2449 {
  final int id;
  const Model2449(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2450 {
  final int id;
  const Model2450(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2451 {
  final int id;
  const Model2451(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2452 {
  final int id;
  const Model2452(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2453 {
  final int id;
  const Model2453(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2454 {
  final int id;
  const Model2454(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2455 {
  final int id;
  const Model2455(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2456 {
  final int id;
  const Model2456(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2457 {
  final int id;
  const Model2457(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2458 {
  final int id;
  const Model2458(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2459 {
  final int id;
  const Model2459(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2460 {
  final int id;
  const Model2460(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2461 {
  final int id;
  const Model2461(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2462 {
  final int id;
  const Model2462(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2463 {
  final int id;
  const Model2463(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2464 {
  final int id;
  const Model2464(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2465 {
  final int id;
  const Model2465(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2466 {
  final int id;
  const Model2466(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2467 {
  final int id;
  const Model2467(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2468 {
  final int id;
  const Model2468(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2469 {
  final int id;
  const Model2469(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2470 {
  final int id;
  const Model2470(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2471 {
  final int id;
  const Model2471(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2472 {
  final int id;
  const Model2472(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2473 {
  final int id;
  const Model2473(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2474 {
  final int id;
  const Model2474(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2475 {
  final int id;
  const Model2475(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2476 {
  final int id;
  const Model2476(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2477 {
  final int id;
  const Model2477(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2478 {
  final int id;
  const Model2478(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2479 {
  final int id;
  const Model2479(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2480 {
  final int id;
  const Model2480(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2481 {
  final int id;
  const Model2481(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2482 {
  final int id;
  const Model2482(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2483 {
  final int id;
  const Model2483(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2484 {
  final int id;
  const Model2484(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2485 {
  final int id;
  const Model2485(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2486 {
  final int id;
  const Model2486(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2487 {
  final int id;
  const Model2487(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2488 {
  final int id;
  const Model2488(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2489 {
  final int id;
  const Model2489(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2490 {
  final int id;
  const Model2490(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2491 {
  final int id;
  const Model2491(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2492 {
  final int id;
  const Model2492(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2493 {
  final int id;
  const Model2493(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2494 {
  final int id;
  const Model2494(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2495 {
  final int id;
  const Model2495(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2496 {
  final int id;
  const Model2496(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2497 {
  final int id;
  const Model2497(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2498 {
  final int id;
  const Model2498(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2499 {
  final int id;
  const Model2499(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2500 {
  final int id;
  const Model2500(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2501 {
  final int id;
  const Model2501(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2502 {
  final int id;
  const Model2502(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2503 {
  final int id;
  const Model2503(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2504 {
  final int id;
  const Model2504(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2505 {
  final int id;
  const Model2505(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 100 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2506 {
  final int id;
  const Model2506(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2507 {
  final int id;
  const Model2507(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2508 {
  final int id;
  const Model2508(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2509 {
  final int id;
  const Model2509(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2510 {
  final int id;
  const Model2510(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2511 {
  final int id;
  const Model2511(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2512 {
  final int id;
  const Model2512(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2513 {
  final int id;
  const Model2513(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2514 {
  final int id;
  const Model2514(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 73 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2515 {
  final int id;
  const Model2515(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2516 {
  final int id;
  const Model2516(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2517 {
  final int id;
  const Model2517(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2518 {
  final int id;
  const Model2518(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2519 {
  final int id;
  const Model2519(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2520 {
  final int id;
  const Model2520(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2521 {
  final int id;
  const Model2521(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2522 {
  final int id;
  const Model2522(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2523 {
  final int id;
  const Model2523(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2524 {
  final int id;
  const Model2524(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 90 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2525 {
  final int id;
  const Model2525(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2526 {
  final int id;
  const Model2526(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2527 {
  final int id;
  const Model2527(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2528 {
  final int id;
  const Model2528(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2529 {
  final int id;
  const Model2529(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2530 {
  final int id;
  const Model2530(this.id);
  int compute(List<int> xs) {
    var acc = 32;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2531 {
  final int id;
  const Model2531(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2532 {
  final int id;
  const Model2532(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2533 {
  final int id;
  const Model2533(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2534 {
  final int id;
  const Model2534(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2535 {
  final int id;
  const Model2535(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2536 {
  final int id;
  const Model2536(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2537 {
  final int id;
  const Model2537(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2538 {
  final int id;
  const Model2538(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2539 {
  final int id;
  const Model2539(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2540 {
  final int id;
  const Model2540(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2541 {
  final int id;
  const Model2541(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2542 {
  final int id;
  const Model2542(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2543 {
  final int id;
  const Model2543(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2544 {
  final int id;
  const Model2544(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2545 {
  final int id;
  const Model2545(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2546 {
  final int id;
  const Model2546(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2547 {
  final int id;
  const Model2547(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2548 {
  final int id;
  const Model2548(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2549 {
  final int id;
  const Model2549(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2550 {
  final int id;
  const Model2550(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2551 {
  final int id;
  const Model2551(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2552 {
  final int id;
  const Model2552(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2553 {
  final int id;
  const Model2553(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2554 {
  final int id;
  const Model2554(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2555 {
  final int id;
  const Model2555(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2556 {
  final int id;
  const Model2556(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2557 {
  final int id;
  const Model2557(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2558 {
  final int id;
  const Model2558(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2559 {
  final int id;
  const Model2559(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2560 {
  final int id;
  const Model2560(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2561 {
  final int id;
  const Model2561(this.id);
  int compute(List<int> xs) {
    var acc = 79;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2562 {
  final int id;
  const Model2562(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2563 {
  final int id;
  const Model2563(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2564 {
  final int id;
  const Model2564(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2565 {
  final int id;
  const Model2565(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2566 {
  final int id;
  const Model2566(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2567 {
  final int id;
  const Model2567(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2568 {
  final int id;
  const Model2568(this.id);
  int compute(List<int> xs) {
    var acc = 2;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2569 {
  final int id;
  const Model2569(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2570 {
  final int id;
  const Model2570(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2571 {
  final int id;
  const Model2571(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2572 {
  final int id;
  const Model2572(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2573 {
  final int id;
  const Model2573(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2574 {
  final int id;
  const Model2574(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2575 {
  final int id;
  const Model2575(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2576 {
  final int id;
  const Model2576(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2577 {
  final int id;
  const Model2577(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 37 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2578 {
  final int id;
  const Model2578(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2579 {
  final int id;
  const Model2579(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2580 {
  final int id;
  const Model2580(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2581 {
  final int id;
  const Model2581(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2582 {
  final int id;
  const Model2582(this.id);
  int compute(List<int> xs) {
    var acc = 7;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2583 {
  final int id;
  const Model2583(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2584 {
  final int id;
  const Model2584(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2585 {
  final int id;
  const Model2585(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2586 {
  final int id;
  const Model2586(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2587 {
  final int id;
  const Model2587(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2588 {
  final int id;
  const Model2588(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2589 {
  final int id;
  const Model2589(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 127 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2590 {
  final int id;
  const Model2590(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2591 {
  final int id;
  const Model2591(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2592 {
  final int id;
  const Model2592(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2593 {
  final int id;
  const Model2593(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2594 {
  final int id;
  const Model2594(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2595 {
  final int id;
  const Model2595(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2596 {
  final int id;
  const Model2596(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2597 {
  final int id;
  const Model2597(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2598 {
  final int id;
  const Model2598(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2599 {
  final int id;
  const Model2599(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2600 {
  final int id;
  const Model2600(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2601 {
  final int id;
  const Model2601(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2602 {
  final int id;
  const Model2602(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2603 {
  final int id;
  const Model2603(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2604 {
  final int id;
  const Model2604(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2605 {
  final int id;
  const Model2605(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2606 {
  final int id;
  const Model2606(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2607 {
  final int id;
  const Model2607(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2608 {
  final int id;
  const Model2608(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2609 {
  final int id;
  const Model2609(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2610 {
  final int id;
  const Model2610(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 35 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2611 {
  final int id;
  const Model2611(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2612 {
  final int id;
  const Model2612(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2613 {
  final int id;
  const Model2613(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2614 {
  final int id;
  const Model2614(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2615 {
  final int id;
  const Model2615(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2616 {
  final int id;
  const Model2616(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 102 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2617 {
  final int id;
  const Model2617(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2618 {
  final int id;
  const Model2618(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2619 {
  final int id;
  const Model2619(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2620 {
  final int id;
  const Model2620(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2621 {
  final int id;
  const Model2621(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2622 {
  final int id;
  const Model2622(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2623 {
  final int id;
  const Model2623(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2624 {
  final int id;
  const Model2624(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2625 {
  final int id;
  const Model2625(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2626 {
  final int id;
  const Model2626(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2627 {
  final int id;
  const Model2627(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2628 {
  final int id;
  const Model2628(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 107 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2629 {
  final int id;
  const Model2629(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2630 {
  final int id;
  const Model2630(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2631 {
  final int id;
  const Model2631(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2632 {
  final int id;
  const Model2632(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2633 {
  final int id;
  const Model2633(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2634 {
  final int id;
  const Model2634(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2635 {
  final int id;
  const Model2635(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2636 {
  final int id;
  const Model2636(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2637 {
  final int id;
  const Model2637(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2638 {
  final int id;
  const Model2638(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2639 {
  final int id;
  const Model2639(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2640 {
  final int id;
  const Model2640(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2641 {
  final int id;
  const Model2641(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2642 {
  final int id;
  const Model2642(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2643 {
  final int id;
  const Model2643(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2644 {
  final int id;
  const Model2644(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2645 {
  final int id;
  const Model2645(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2646 {
  final int id;
  const Model2646(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2647 {
  final int id;
  const Model2647(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2648 {
  final int id;
  const Model2648(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2649 {
  final int id;
  const Model2649(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2650 {
  final int id;
  const Model2650(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2651 {
  final int id;
  const Model2651(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2652 {
  final int id;
  const Model2652(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2653 {
  final int id;
  const Model2653(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2654 {
  final int id;
  const Model2654(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2655 {
  final int id;
  const Model2655(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2656 {
  final int id;
  const Model2656(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2657 {
  final int id;
  const Model2657(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2658 {
  final int id;
  const Model2658(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2659 {
  final int id;
  const Model2659(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2660 {
  final int id;
  const Model2660(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2661 {
  final int id;
  const Model2661(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2662 {
  final int id;
  const Model2662(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2663 {
  final int id;
  const Model2663(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2664 {
  final int id;
  const Model2664(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2665 {
  final int id;
  const Model2665(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 97 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2666 {
  final int id;
  const Model2666(this.id);
  int compute(List<int> xs) {
    var acc = 39;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2667 {
  final int id;
  const Model2667(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2668 {
  final int id;
  const Model2668(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2669 {
  final int id;
  const Model2669(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2670 {
  final int id;
  const Model2670(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2671 {
  final int id;
  const Model2671(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2672 {
  final int id;
  const Model2672(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2673 {
  final int id;
  const Model2673(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2674 {
  final int id;
  const Model2674(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2675 {
  final int id;
  const Model2675(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2676 {
  final int id;
  const Model2676(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2677 {
  final int id;
  const Model2677(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 74 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2678 {
  final int id;
  const Model2678(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2679 {
  final int id;
  const Model2679(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2680 {
  final int id;
  const Model2680(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2681 {
  final int id;
  const Model2681(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2682 {
  final int id;
  const Model2682(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2683 {
  final int id;
  const Model2683(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2684 {
  final int id;
  const Model2684(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2685 {
  final int id;
  const Model2685(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2686 {
  final int id;
  const Model2686(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2687 {
  final int id;
  const Model2687(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2688 {
  final int id;
  const Model2688(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2689 {
  final int id;
  const Model2689(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2690 {
  final int id;
  const Model2690(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2691 {
  final int id;
  const Model2691(this.id);
  int compute(List<int> xs) {
    var acc = 51;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2692 {
  final int id;
  const Model2692(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2693 {
  final int id;
  const Model2693(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2694 {
  final int id;
  const Model2694(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2695 {
  final int id;
  const Model2695(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2696 {
  final int id;
  const Model2696(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2697 {
  final int id;
  const Model2697(this.id);
  int compute(List<int> xs) {
    var acc = 13;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2698 {
  final int id;
  const Model2698(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2699 {
  final int id;
  const Model2699(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2700 {
  final int id;
  const Model2700(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2701 {
  final int id;
  const Model2701(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2702 {
  final int id;
  const Model2702(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2703 {
  final int id;
  const Model2703(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2704 {
  final int id;
  const Model2704(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2705 {
  final int id;
  const Model2705(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2706 {
  final int id;
  const Model2706(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 10 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2707 {
  final int id;
  const Model2707(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2708 {
  final int id;
  const Model2708(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2709 {
  final int id;
  const Model2709(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2710 {
  final int id;
  const Model2710(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2711 {
  final int id;
  const Model2711(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2712 {
  final int id;
  const Model2712(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2713 {
  final int id;
  const Model2713(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2714 {
  final int id;
  const Model2714(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2715 {
  final int id;
  const Model2715(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2716 {
  final int id;
  const Model2716(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2717 {
  final int id;
  const Model2717(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2718 {
  final int id;
  const Model2718(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2719 {
  final int id;
  const Model2719(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2720 {
  final int id;
  const Model2720(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2721 {
  final int id;
  const Model2721(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2722 {
  final int id;
  const Model2722(this.id);
  int compute(List<int> xs) {
    var acc = 37;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2723 {
  final int id;
  const Model2723(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 34 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2724 {
  final int id;
  const Model2724(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2725 {
  final int id;
  const Model2725(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2726 {
  final int id;
  const Model2726(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2727 {
  final int id;
  const Model2727(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2728 {
  final int id;
  const Model2728(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2729 {
  final int id;
  const Model2729(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2730 {
  final int id;
  const Model2730(this.id);
  int compute(List<int> xs) {
    var acc = 60;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2731 {
  final int id;
  const Model2731(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2732 {
  final int id;
  const Model2732(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2733 {
  final int id;
  const Model2733(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2734 {
  final int id;
  const Model2734(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2735 {
  final int id;
  const Model2735(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2736 {
  final int id;
  const Model2736(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2737 {
  final int id;
  const Model2737(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2738 {
  final int id;
  const Model2738(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2739 {
  final int id;
  const Model2739(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2740 {
  final int id;
  const Model2740(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2741 {
  final int id;
  const Model2741(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2742 {
  final int id;
  const Model2742(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 64 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2743 {
  final int id;
  const Model2743(this.id);
  int compute(List<int> xs) {
    var acc = 30;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2744 {
  final int id;
  const Model2744(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2745 {
  final int id;
  const Model2745(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 70 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2746 {
  final int id;
  const Model2746(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2747 {
  final int id;
  const Model2747(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2748 {
  final int id;
  const Model2748(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2749 {
  final int id;
  const Model2749(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2750 {
  final int id;
  const Model2750(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2751 {
  final int id;
  const Model2751(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2752 {
  final int id;
  const Model2752(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 82 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2753 {
  final int id;
  const Model2753(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 41 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2754 {
  final int id;
  const Model2754(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2755 {
  final int id;
  const Model2755(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2756 {
  final int id;
  const Model2756(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2757 {
  final int id;
  const Model2757(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2758 {
  final int id;
  const Model2758(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2759 {
  final int id;
  const Model2759(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2760 {
  final int id;
  const Model2760(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2761 {
  final int id;
  const Model2761(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2762 {
  final int id;
  const Model2762(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 55 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2763 {
  final int id;
  const Model2763(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2764 {
  final int id;
  const Model2764(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2765 {
  final int id;
  const Model2765(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2766 {
  final int id;
  const Model2766(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2767 {
  final int id;
  const Model2767(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2768 {
  final int id;
  const Model2768(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2769 {
  final int id;
  const Model2769(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2770 {
  final int id;
  const Model2770(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2771 {
  final int id;
  const Model2771(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2772 {
  final int id;
  const Model2772(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2773 {
  final int id;
  const Model2773(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2774 {
  final int id;
  const Model2774(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 86 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2775 {
  final int id;
  const Model2775(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2776 {
  final int id;
  const Model2776(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2777 {
  final int id;
  const Model2777(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2778 {
  final int id;
  const Model2778(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2779 {
  final int id;
  const Model2779(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2780 {
  final int id;
  const Model2780(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 21 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2781 {
  final int id;
  const Model2781(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2782 {
  final int id;
  const Model2782(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2783 {
  final int id;
  const Model2783(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2784 {
  final int id;
  const Model2784(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2785 {
  final int id;
  const Model2785(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2786 {
  final int id;
  const Model2786(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2787 {
  final int id;
  const Model2787(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 87 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2788 {
  final int id;
  const Model2788(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2789 {
  final int id;
  const Model2789(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2790 {
  final int id;
  const Model2790(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2791 {
  final int id;
  const Model2791(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2792 {
  final int id;
  const Model2792(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2793 {
  final int id;
  const Model2793(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2794 {
  final int id;
  const Model2794(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2795 {
  final int id;
  const Model2795(this.id);
  int compute(List<int> xs) {
    var acc = 17;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2796 {
  final int id;
  const Model2796(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2797 {
  final int id;
  const Model2797(this.id);
  int compute(List<int> xs) {
    var acc = 9;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2798 {
  final int id;
  const Model2798(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2799 {
  final int id;
  const Model2799(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2800 {
  final int id;
  const Model2800(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2801 {
  final int id;
  const Model2801(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2802 {
  final int id;
  const Model2802(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2803 {
  final int id;
  const Model2803(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 104 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2804 {
  final int id;
  const Model2804(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2805 {
  final int id;
  const Model2805(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2806 {
  final int id;
  const Model2806(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2807 {
  final int id;
  const Model2807(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2808 {
  final int id;
  const Model2808(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2809 {
  final int id;
  const Model2809(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2810 {
  final int id;
  const Model2810(this.id);
  int compute(List<int> xs) {
    var acc = 57;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2811 {
  final int id;
  const Model2811(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2812 {
  final int id;
  const Model2812(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2813 {
  final int id;
  const Model2813(this.id);
  int compute(List<int> xs) {
    var acc = 23;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2814 {
  final int id;
  const Model2814(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2815 {
  final int id;
  const Model2815(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2816 {
  final int id;
  const Model2816(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2817 {
  final int id;
  const Model2817(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2818 {
  final int id;
  const Model2818(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2819 {
  final int id;
  const Model2819(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 125 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2820 {
  final int id;
  const Model2820(this.id);
  int compute(List<int> xs) {
    var acc = 18;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2821 {
  final int id;
  const Model2821(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2822 {
  final int id;
  const Model2822(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2823 {
  final int id;
  const Model2823(this.id);
  int compute(List<int> xs) {
    var acc = 86;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2824 {
  final int id;
  const Model2824(this.id);
  int compute(List<int> xs) {
    var acc = 74;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2825 {
  final int id;
  const Model2825(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2826 {
  final int id;
  const Model2826(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2827 {
  final int id;
  const Model2827(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2828 {
  final int id;
  const Model2828(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2829 {
  final int id;
  const Model2829(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 33 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2830 {
  final int id;
  const Model2830(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 57 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2831 {
  final int id;
  const Model2831(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2832 {
  final int id;
  const Model2832(this.id);
  int compute(List<int> xs) {
    var acc = 14;
    for (final x in xs) { acc = acc * 94 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2833 {
  final int id;
  const Model2833(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2834 {
  final int id;
  const Model2834(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2835 {
  final int id;
  const Model2835(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2836 {
  final int id;
  const Model2836(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 69 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2837 {
  final int id;
  const Model2837(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2838 {
  final int id;
  const Model2838(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2839 {
  final int id;
  const Model2839(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 46 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2840 {
  final int id;
  const Model2840(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 26 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2841 {
  final int id;
  const Model2841(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2842 {
  final int id;
  const Model2842(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2843 {
  final int id;
  const Model2843(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 124 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2844 {
  final int id;
  const Model2844(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2845 {
  final int id;
  const Model2845(this.id);
  int compute(List<int> xs) {
    var acc = 28;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2846 {
  final int id;
  const Model2846(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2847 {
  final int id;
  const Model2847(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2848 {
  final int id;
  const Model2848(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2849 {
  final int id;
  const Model2849(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2850 {
  final int id;
  const Model2850(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2851 {
  final int id;
  const Model2851(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 72 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2852 {
  final int id;
  const Model2852(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2853 {
  final int id;
  const Model2853(this.id);
  int compute(List<int> xs) {
    var acc = 66;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2854 {
  final int id;
  const Model2854(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2855 {
  final int id;
  const Model2855(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 31 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2856 {
  final int id;
  const Model2856(this.id);
  int compute(List<int> xs) {
    var acc = 45;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2857 {
  final int id;
  const Model2857(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 48 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2858 {
  final int id;
  const Model2858(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2859 {
  final int id;
  const Model2859(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 114 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2860 {
  final int id;
  const Model2860(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2861 {
  final int id;
  const Model2861(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2862 {
  final int id;
  const Model2862(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 23 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2863 {
  final int id;
  const Model2863(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2864 {
  final int id;
  const Model2864(this.id);
  int compute(List<int> xs) {
    var acc = 70;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2865 {
  final int id;
  const Model2865(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2866 {
  final int id;
  const Model2866(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2867 {
  final int id;
  const Model2867(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 92 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2868 {
  final int id;
  const Model2868(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 11 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2869 {
  final int id;
  const Model2869(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 99 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2870 {
  final int id;
  const Model2870(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2871 {
  final int id;
  const Model2871(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2872 {
  final int id;
  const Model2872(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2873 {
  final int id;
  const Model2873(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2874 {
  final int id;
  const Model2874(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2875 {
  final int id;
  const Model2875(this.id);
  int compute(List<int> xs) {
    var acc = 73;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2876 {
  final int id;
  const Model2876(this.id);
  int compute(List<int> xs) {
    var acc = 47;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2877 {
  final int id;
  const Model2877(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2878 {
  final int id;
  const Model2878(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2879 {
  final int id;
  const Model2879(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 22 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2880 {
  final int id;
  const Model2880(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2881 {
  final int id;
  const Model2881(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 78 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2882 {
  final int id;
  const Model2882(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2883 {
  final int id;
  const Model2883(this.id);
  int compute(List<int> xs) {
    var acc = 6;
    for (final x in xs) { acc = acc * 7 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2884 {
  final int id;
  const Model2884(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2885 {
  final int id;
  const Model2885(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 25 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2886 {
  final int id;
  const Model2886(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 116 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2887 {
  final int id;
  const Model2887(this.id);
  int compute(List<int> xs) {
    var acc = 33;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2888 {
  final int id;
  const Model2888(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2889 {
  final int id;
  const Model2889(this.id);
  int compute(List<int> xs) {
    var acc = 93;
    for (final x in xs) { acc = acc * 13 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2890 {
  final int id;
  const Model2890(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2891 {
  final int id;
  const Model2891(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2892 {
  final int id;
  const Model2892(this.id);
  int compute(List<int> xs) {
    var acc = 31;
    for (final x in xs) { acc = acc * 28 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2893 {
  final int id;
  const Model2893(this.id);
  int compute(List<int> xs) {
    var acc = 52;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2894 {
  final int id;
  const Model2894(this.id);
  int compute(List<int> xs) {
    var acc = 46;
    for (final x in xs) { acc = acc * 96 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2895 {
  final int id;
  const Model2895(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 76 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2896 {
  final int id;
  const Model2896(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 59 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2897 {
  final int id;
  const Model2897(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2898 {
  final int id;
  const Model2898(this.id);
  int compute(List<int> xs) {
    var acc = 11;
    for (final x in xs) { acc = acc * 65 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2899 {
  final int id;
  const Model2899(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2900 {
  final int id;
  const Model2900(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 20 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2901 {
  final int id;
  const Model2901(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2902 {
  final int id;
  const Model2902(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2903 {
  final int id;
  const Model2903(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2904 {
  final int id;
  const Model2904(this.id);
  int compute(List<int> xs) {
    var acc = 12;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2905 {
  final int id;
  const Model2905(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 85 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2906 {
  final int id;
  const Model2906(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2907 {
  final int id;
  const Model2907(this.id);
  int compute(List<int> xs) {
    var acc = 82;
    for (final x in xs) { acc = acc * 95 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2908 {
  final int id;
  const Model2908(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2909 {
  final int id;
  const Model2909(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2910 {
  final int id;
  const Model2910(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 8 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2911 {
  final int id;
  const Model2911(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 32 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2912 {
  final int id;
  const Model2912(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2913 {
  final int id;
  const Model2913(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 79 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2914 {
  final int id;
  const Model2914(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2915 {
  final int id;
  const Model2915(this.id);
  int compute(List<int> xs) {
    var acc = 4;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2916 {
  final int id;
  const Model2916(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2917 {
  final int id;
  const Model2917(this.id);
  int compute(List<int> xs) {
    var acc = 91;
    for (final x in xs) { acc = acc * 120 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2918 {
  final int id;
  const Model2918(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2919 {
  final int id;
  const Model2919(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2920 {
  final int id;
  const Model2920(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2921 {
  final int id;
  const Model2921(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 75 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2922 {
  final int id;
  const Model2922(this.id);
  int compute(List<int> xs) {
    var acc = 88;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2923 {
  final int id;
  const Model2923(this.id);
  int compute(List<int> xs) {
    var acc = 67;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2924 {
  final int id;
  const Model2924(this.id);
  int compute(List<int> xs) {
    var acc = 54;
    for (final x in xs) { acc = acc * 131 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2925 {
  final int id;
  const Model2925(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 71 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2926 {
  final int id;
  const Model2926(this.id);
  int compute(List<int> xs) {
    var acc = 21;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2927 {
  final int id;
  const Model2927(this.id);
  int compute(List<int> xs) {
    var acc = 3;
    for (final x in xs) { acc = acc * 29 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2928 {
  final int id;
  const Model2928(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2929 {
  final int id;
  const Model2929(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 56 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2930 {
  final int id;
  const Model2930(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 106 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2931 {
  final int id;
  const Model2931(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 122 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2932 {
  final int id;
  const Model2932(this.id);
  int compute(List<int> xs) {
    var acc = 97;
    for (final x in xs) { acc = acc * 18 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2933 {
  final int id;
  const Model2933(this.id);
  int compute(List<int> xs) {
    var acc = 64;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2934 {
  final int id;
  const Model2934(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 130 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2935 {
  final int id;
  const Model2935(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 101 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2936 {
  final int id;
  const Model2936(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 50 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2937 {
  final int id;
  const Model2937(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 80 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2938 {
  final int id;
  const Model2938(this.id);
  int compute(List<int> xs) {
    var acc = 49;
    for (final x in xs) { acc = acc * 84 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2939 {
  final int id;
  const Model2939(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2940 {
  final int id;
  const Model2940(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2941 {
  final int id;
  const Model2941(this.id);
  int compute(List<int> xs) {
    var acc = 59;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2942 {
  final int id;
  const Model2942(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2943 {
  final int id;
  const Model2943(this.id);
  int compute(List<int> xs) {
    var acc = 25;
    for (final x in xs) { acc = acc * 58 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2944 {
  final int id;
  const Model2944(this.id);
  int compute(List<int> xs) {
    var acc = 44;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2945 {
  final int id;
  const Model2945(this.id);
  int compute(List<int> xs) {
    var acc = 77;
    for (final x in xs) { acc = acc * 44 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2946 {
  final int id;
  const Model2946(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2947 {
  final int id;
  const Model2947(this.id);
  int compute(List<int> xs) {
    var acc = 50;
    for (final x in xs) { acc = acc * 113 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2948 {
  final int id;
  const Model2948(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 66 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2949 {
  final int id;
  const Model2949(this.id);
  int compute(List<int> xs) {
    var acc = 63;
    for (final x in xs) { acc = acc * 109 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2950 {
  final int id;
  const Model2950(this.id);
  int compute(List<int> xs) {
    var acc = 24;
    for (final x in xs) { acc = acc * 60 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2951 {
  final int id;
  const Model2951(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2952 {
  final int id;
  const Model2952(this.id);
  int compute(List<int> xs) {
    var acc = 69;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2953 {
  final int id;
  const Model2953(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 36 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2954 {
  final int id;
  const Model2954(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2955 {
  final int id;
  const Model2955(this.id);
  int compute(List<int> xs) {
    var acc = 61;
    for (final x in xs) { acc = acc * 117 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2956 {
  final int id;
  const Model2956(this.id);
  int compute(List<int> xs) {
    var acc = 35;
    for (final x in xs) { acc = acc * 121 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2957 {
  final int id;
  const Model2957(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 81 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2958 {
  final int id;
  const Model2958(this.id);
  int compute(List<int> xs) {
    var acc = 19;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2959 {
  final int id;
  const Model2959(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 16 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2960 {
  final int id;
  const Model2960(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 30 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2961 {
  final int id;
  const Model2961(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2962 {
  final int id;
  const Model2962(this.id);
  int compute(List<int> xs) {
    var acc = 62;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2963 {
  final int id;
  const Model2963(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2964 {
  final int id;
  const Model2964(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 112 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2965 {
  final int id;
  const Model2965(this.id);
  int compute(List<int> xs) {
    var acc = 87;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2966 {
  final int id;
  const Model2966(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 12 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2967 {
  final int id;
  const Model2967(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 128 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2968 {
  final int id;
  const Model2968(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 123 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2969 {
  final int id;
  const Model2969(this.id);
  int compute(List<int> xs) {
    var acc = 29;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2970 {
  final int id;
  const Model2970(this.id);
  int compute(List<int> xs) {
    var acc = 81;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2971 {
  final int id;
  const Model2971(this.id);
  int compute(List<int> xs) {
    var acc = 36;
    for (final x in xs) { acc = acc * 129 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2972 {
  final int id;
  const Model2972(this.id);
  int compute(List<int> xs) {
    var acc = 85;
    for (final x in xs) { acc = acc * 77 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2973 {
  final int id;
  const Model2973(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 89 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2974 {
  final int id;
  const Model2974(this.id);
  int compute(List<int> xs) {
    var acc = 41;
    for (final x in xs) { acc = acc * 63 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2975 {
  final int id;
  const Model2975(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 67 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2976 {
  final int id;
  const Model2976(this.id);
  int compute(List<int> xs) {
    var acc = 75;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2977 {
  final int id;
  const Model2977(this.id);
  int compute(List<int> xs) {
    var acc = 68;
    for (final x in xs) { acc = acc * 54 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2978 {
  final int id;
  const Model2978(this.id);
  int compute(List<int> xs) {
    var acc = 5;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2979 {
  final int id;
  const Model2979(this.id);
  int compute(List<int> xs) {
    var acc = 80;
    for (final x in xs) { acc = acc * 4 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2980 {
  final int id;
  const Model2980(this.id);
  int compute(List<int> xs) {
    var acc = 48;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model2981 {
  final int id;
  const Model2981(this.id);
  int compute(List<int> xs) {
    var acc = 27;
    for (final x in xs) { acc = acc * 119 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2982 {
  final int id;
  const Model2982(this.id);
  int compute(List<int> xs) {
    var acc = 8;
    for (final x in xs) { acc = acc * 42 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2983 {
  final int id;
  const Model2983(this.id);
  int compute(List<int> xs) {
    var acc = 15;
    for (final x in xs) { acc = acc * 14 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2984 {
  final int id;
  const Model2984(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 45 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model2985 {
  final int id;
  const Model2985(this.id);
  int compute(List<int> xs) {
    var acc = 20;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2986 {
  final int id;
  const Model2986(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 93 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2987 {
  final int id;
  const Model2987(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 39 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2988 {
  final int id;
  const Model2988(this.id);
  int compute(List<int> xs) {
    var acc = 55;
    for (final x in xs) { acc = acc * 43 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2989 {
  final int id;
  const Model2989(this.id);
  int compute(List<int> xs) {
    var acc = 71;
    for (final x in xs) { acc = acc * 3 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2990 {
  final int id;
  const Model2990(this.id);
  int compute(List<int> xs) {
    var acc = 22;
    for (final x in xs) { acc = acc * 61 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2991 {
  final int id;
  const Model2991(this.id);
  int compute(List<int> xs) {
    var acc = 65;
    for (final x in xs) { acc = acc * 49 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model2992 {
  final int id;
  const Model2992(this.id);
  int compute(List<int> xs) {
    var acc = 26;
    for (final x in xs) { acc = acc * 27 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model2993 {
  final int id;
  const Model2993(this.id);
  int compute(List<int> xs) {
    var acc = 43;
    for (final x in xs) { acc = acc * 9 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2994 {
  final int id;
  const Model2994(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 47 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model2995 {
  final int id;
  const Model2995(this.id);
  int compute(List<int> xs) {
    var acc = 95;
    for (final x in xs) { acc = acc * 51 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model2996 {
  final int id;
  const Model2996(this.id);
  int compute(List<int> xs) {
    var acc = 10;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model2997 {
  final int id;
  const Model2997(this.id);
  int compute(List<int> xs) {
    var acc = 42;
    for (final x in xs) { acc = acc * 105 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model2998 {
  final int id;
  const Model2998(this.id);
  int compute(List<int> xs) {
    var acc = 40;
    for (final x in xs) { acc = acc * 15 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model2999 {
  final int id;
  const Model2999(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 53 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 2}";
}

class Model3000 {
  final int id;
  const Model3000(this.id);
  int compute(List<int> xs) {
    var acc = 96;
    for (final x in xs) { acc = acc * 111 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model3001 {
  final int id;
  const Model3001(this.id);
  int compute(List<int> xs) {
    var acc = 94;
    for (final x in xs) { acc = acc * 6 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model3002 {
  final int id;
  const Model3002(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 38 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model3003 {
  final int id;
  const Model3003(this.id);
  int compute(List<int> xs) {
    var acc = 78;
    for (final x in xs) { acc = acc * 118 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model3004 {
  final int id;
  const Model3004(this.id);
  int compute(List<int> xs) {
    var acc = 76;
    for (final x in xs) { acc = acc * 5 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model3005 {
  final int id;
  const Model3005(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 68 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model3006 {
  final int id;
  const Model3006(this.id);
  int compute(List<int> xs) {
    var acc = 92;
    for (final x in xs) { acc = acc * 103 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model3007 {
  final int id;
  const Model3007(this.id);
  int compute(List<int> xs) {
    var acc = 83;
    for (final x in xs) { acc = acc * 40 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model3008 {
  final int id;
  const Model3008(this.id);
  int compute(List<int> xs) {
    var acc = 34;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 4}";
}

class Model3009 {
  final int id;
  const Model3009(this.id);
  int compute(List<int> xs) {
    var acc = 72;
    for (final x in xs) { acc = acc * 110 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 5}";
}

class Model3010 {
  final int id;
  const Model3010(this.id);
  int compute(List<int> xs) {
    var acc = 90;
    for (final x in xs) { acc = acc * 98 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}

class Model3011 {
  final int id;
  const Model3011(this.id);
  int compute(List<int> xs) {
    var acc = 84;
    for (final x in xs) { acc = acc * 83 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model3012 {
  final int id;
  const Model3012(this.id);
  int compute(List<int> xs) {
    var acc = 53;
    for (final x in xs) { acc = acc * 108 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 9}";
}

class Model3013 {
  final int id;
  const Model3013(this.id);
  int compute(List<int> xs) {
    var acc = 16;
    for (final x in xs) { acc = acc * 52 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model3014 {
  final int id;
  const Model3014(this.id);
  int compute(List<int> xs) {
    var acc = 58;
    for (final x in xs) { acc = acc * 91 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 3}";
}

class Model3015 {
  final int id;
  const Model3015(this.id);
  int compute(List<int> xs) {
    var acc = 38;
    for (final x in xs) { acc = acc * 17 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 1}";
}

class Model3016 {
  final int id;
  const Model3016(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 88 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 7}";
}

class Model3017 {
  final int id;
  const Model3017(this.id);
  int compute(List<int> xs) {
    var acc = 56;
    for (final x in xs) { acc = acc * 115 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 8}";
}

class Model3018 {
  final int id;
  const Model3018(this.id);
  int compute(List<int> xs) {
    var acc = 89;
    for (final x in xs) { acc = acc * 126 + x * id; }
    return acc & 0x7fffffff;
  }
  String label() => "model-${id * 6}";
}
// xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
